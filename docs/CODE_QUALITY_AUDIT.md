<!-- §0–§2: як мірялося, критерії, оцінка -->
# Аудит якості: оцінка за професійною системою

**Дата:** 12.09.2026 · **Звірено з кодом:** 12.09.2026 · **Метод:** статичні виміри
(`wc -l`, `grep`, обхід графа імпортів, `tsc`, `eslint`, `vitest`, `vite build`, `npm audit`).

> **Підсумок: 57,9% порядку** (578,5 з 1000 зважених балів). Документація — **88%**
> (окрема оцінка, §5). Борг зосереджений не там, де його зазвичай шукають: процес,
> CI й документація — сильні; тягнуть вниз **вимірюваність**, **тести критичних
> шляхів**, **дизайн-система всередині блоків** і **мобільна доступність**.
>
> **Ця цифра — не вирок, а адреса.** Кожен бал нижче має конкретне місце в коді,
> яке видно очима (§4), і пункт плану, який його піднімає (§7).

---

## 0. Як саме мірялося (щоб цифру можна було перевірити)

| Що | Як |
|---|---|
| Мертвий код | обхід графа імпортів від точок входу (`main.tsx`, `worker.ts`, `router.tsx`, `exports` у `package.json` пакетів), включно з динамічними `import()` |
| Спільний код ↔ CSS | множина класів, які рендерить `packages/ui`, проти множини селекторів усіх `.css` |
| Мертвий CSS | клас, якого немає в `src/` **навіть як підрядок** (це **нижня** оцінка: динамічно склеєні імена невидимі статично) |
| Покриття тестами | не coverage-інструмент, а граф: скільки модулів `src/` імпортується з тестів |
| Вага бандла | `ls -la` по `dist/` після `vite build` обох оболонок |
| Уразливості | `npm audit` |

**Чого цей аудит НЕ містить:** вимірювання продуктивності в рантаймі, візуальної
перевірки, E2E-тестів, пентесту, аналізу користувацьких сценаріїв на пристроях.
Нічого з цього в середовищі аудиту зробити неможливо — і це прямо знижує впевненість
у критеріях 6 (мобільність) і 9 (продуктивність).

**Зміна, яку аудит уже спричинив:** знайдено й видалено **74 недосяжні файли
(5 238 рядків, 15% коду)** — деталі в §4.1 і в `CONSOLIDATION_LOG.md` §9. Усі цифри
нижче — **після** цього видалення.

---

## 1. Критерії: за якою системою оцінюється

Власної «системи» не вигадувалось. Рубрика зібрана з визнаних моделей, адаптованих
під цей проєкт (Cloudflare Workers + React + D1, мобільний TMA пріоритет №1):

| Джерело | Що взято |
|---|---|
| **ISO/IEC 25010 (SQuaRE)** | характеристики: maintainability, security, reliability, performance efficiency, usability/accessibility, compatibility |
| **CISQ / OMG** measures for maintainability | мертвий код, дублювання, розмір модулів, складність |
| **SIG/TÜViT** maintainability model | розмір файлів (200/400 рядків) як поріг читабельності |
| **WCAG 2.2 AA** | доступність: семантика, фокус, розмір тап-таргетів |
| **`AGENTS.md` §3, §7** | локальні норми проєкту (кристалевість, заборона `SELECT *` на `users`, нативних діалогів, класу без CSS) |
| **DORA / CI-зрілість** | гейти, що блокують деплой; відтворюваність збірки |
| **Bundle budget** | вага чанка як окремий критерій для TWA |

Кожен критерій має вагу і 2–7 перевірок. Перевірка дає **2** (норма виконана),
**1** (частково) або **0** (ні). Оцінка критерію = набрані бали ÷ максимальні × 10.
Підсумок = сума (оцінка × вага) ÷ 100.

**Вага навмисно не рівна.** Мобільна доступність (15) і тести (15) важать більше за
косметику: це єдиний продукт, який бачать користувачі, і єдине, що ловить регресії.

---

## 2. Оцінка

| # | Критерій | Вага | Оцінка | Бал |
|---|---|---:|---:|---:|
| 1 | Кристалевість і структура | 12 | 3,8 / 10 | **45,0** |
| 2 | Типобезпека | 8 | 7,5 / 10 | **60,0** |
| 3 | Тести | 15 | 6,0 / 10 | **90,0** |
| 4 | Мертвий код і гігієна | 8 | 6,3 / 10 | **50,4** |
| 5 | Межі шарів і дублювання | 8 | 6,0 / 10 | **48,0** |
| 6 | Мобільність і доступність | 15 | 5,7 / 10 | **85,5** |
| 7 | Дизайн-система | 10 | 5,0 / 10 | **50,0** |
| 8 | Безпека | 12 | 5,8 / 10 | **69,6** |
| 9 | Продуктивність і вага бандла | 4 | 2,5 / 10 | **10,0** |
| 10 | Документація і процес | 8 | 8,8 / 10 | **70,0** |
| | **Разом** | **100** | | **578,5 → 58%** |

### 2.1 Кристалевість — 3,8

| Перевірка | Факт | Бал |
|---|---|---:|
| Немає файлів >400 рядків | **3 логічних понад 400**: `ScenarioCardModal.tsx` 443, `UserProfileCard.tsx` 417, `UserEditModal.tsx` 401 (ще 3 — дані: `site-templates.ts`, `icons.tsx`, `site.types.ts`). ~~`sites.service.ts` 777~~ — поділено 12.09 на `services/sites/*` (найбільший 178) | 0 |
| Частка файлів >200 ≤10% | **16%** (40 із 255 логічних; ще 3 — дані, ліміт на них не діє) | 0 |
| Нові модулі пишуться за правилом | `site-editor/` — 11 файлів, найбільший 176; `my-dates-table/` — 10 файлів, найбільший 125 | 2 |
| Логіка відокремлена від рендеру | є `use*.ts`, `actions.ts`, `form.ts`, `filter-sort.ts`; але `ScenarioCardModal` (443) і `UserEditModal` (401) досі містять і те, і те | 1 |

### 2.2 Типобезпека — 7,5

`any` — **0** (ESLint + `tsc`) · `strict: true` у всіх 6 воркспейсах (**2+2**).
`as unknown as` — **19** придушень (частково: 1). Валідація на межах: `parsePageConfig`
для `page_data` і `try/catch` на JSON з D1 є, але тіла запитів у контролерах не
валідуються схемою (немає zod/valibot) — частково (1).

### 2.3 Тести — 6,0

**182 тести у 22 файлах**, гейтять CI (**2**): регресії справді ловляться —
`wrangler-env.test.ts` (середовище), `identity.test.ts` (ідентичність),
`session.test.ts` (HMAC) (**2**).

Слабке місце — не якість, а **площа**:

| Перевірка | Факт | Бал |
|---|---|---:|
| Обсяг тестів до коду | 2 012 рядків тестів на 29 992 рядки `src` (нові 100 рядків — JSDoc шести модулів `services/sites/`) | 1 |
| Покриття модулів | лише **24 з 258** модулів імпортуються з тестів (**9%**) | 0 |
| Критичні шляхи | `security/`, `config/`, `datetime`, `condition-evaluator`, `PageRenderer`, `PermissionGate` ✅ · **`services/sites/*` (6 модулів, найбільший 178 — найризикованіший домен) — 0 тестів** ❌ | 1 |

### 2.4 Мертвий код і гігієна — 6,3

**Видалено 74 недосяжні файли** (5 238 рядків) — після цього недосяжних модулів у
`src/` **нуль** (**2**). TODO/FIXME — **1** (**2**).

Далі — борг:

- **≥167 визначених CSS-класів не існують у `src/` навіть як підрядок** (адмінка 53,
  платформа 42, `components.css` 32, по 14 у `apple.css`/`android.css`) — **0**;
- **21 `console.*`** у продакшн-коді поза модулями логування (`bot-dev` 12,
  `web-admin-dev` 5, `api-dev` 3 — там `logger.ts`, це норма) — частково (**1**).

### 2.5 Межі шарів і дублювання — 6,0

Правило «двічі — в спільне» працює: HMAC-сесія й перевірка `initData` — в
`packages/shared/src/security/` (**2**); `apiFetch` — спільний, а обгортки в оболонках
мають **документовану** різницю (`credentials: include` для cookie) — це не копія, а
межа (**2**). Дубль `DateModal` (477 рядків у двох місцях) закрито — лишилась одна.

Тягне вниз те саме, що й §2.7 (спільний код, стилізований не спільно) — **0**, і
`.usr-*` (43 класи) / `.scn-*` (20) в адмінці, які дублюють спільні
`.wb-data-table` / `.wb-modal-*` — частково (**1**).

### 2.6 Мобільність і доступність — 5,7 (вага 15 — ціна помилки найвища)

| Перевірка | Факт | Бал |
|---|---|---:|
| `viewport-fit=cover` | є в обох `index.html` (без нього `env(safe-area-inset-*)` = 0) | 2 |
| `100dvh` замість `100vh` | 24 входження `dvh`; **голих `100vh` — 0**: усі 13 входжень стоять парами з `100dvh`-фолбеком (*поправка 12.09.2026: тут стояло «13 голих» — це була помилка виміру, а не борг*); тепер стереже `check:quality` | 1 |
| `var(--safe-*)` | 17 входжень | 2 |
| Тап-таргети ≥44px | 11 входжень, **без системності** — частина кнопок дрібніша | 1 |
| Афорданси без `:hover` | **`@media (hover: none)` — 1 входження на весь репозиторій** | 0 |
| Нативні діалоги | **0** (33 виклики замінено на `useDialog()` 12.09) | 2 |
| Семантика і a11y | `aria-*` 24, `role` 10, **16 `onClick` на `div/span/li`**, 12 `<select>` | 0 |

### 2.7 Дизайн-система — 5,0

Спільний шар підключено в обох оболонках (**2**), емоджі в UI — **0** (**2**;
*поправка 12.09.2026: на момент написання їх було 16 — знайдено й закрито гейтом
`check:quality`; тепер це результат перевірки, а не оцінка*).
Але:

- у `packages/shared/src/styles/` **218 правил `.wb-*`**, а в адмінці — **238 власних
  класів**; це два різні набори для тих самих елементів (**1**);
- **667 інлайнових `style={{…}}`** і **77 хардкод-`#hex`** у живому `src` (з них у
  `packages/ui` — 192) (**0**);
- **44 класи `wb-block-*`, які рендерить спільний код, не мають жодного правила CSS**
  (`.wb-block-`, `grep` по всіх `.css` = 0 входжень). Отже блоки побудовані інлайном,
  і **`data-brand` / `data-theme` до них не доходять**: 1 354 рядки
  `apple.css` + `android.css` на блоки не впливають узагалі (**0**).

### 2.8 Безпека — 5,8

Сильне: ідентичність — **тільки** з підписаного `initData` (24 виклики
`resolveUserId`/`tryResolveUserId` у контролерах і router) (**2**); адмін — cookie
`admin_session` + гейт, секретів у заголовках немає (**2**); секретів у репозиторії не
знайдено (**2**).

| Перевірка | Факт | Бал |
|---|---|---:|
| Заборона `SELECT *` на важких JSON-таблицях | **порушено двічі**: `users` (`api-dev/src/services/users.service.ts:55`, `bot-dev/src/modules/users/user.repository.ts:17`); усього `SELECT *` — 20 місць | 0 |
| Залежності | `npm audit`: **6 уразливостей (4 high)**; гейт CI стоїть на `--audit-level=critical`, тому **high проходять** | 0 |
| Публічний запис | `/api/mydate/*` пише в D1/KV без `initData` і без rate-limit (відоме, чекає трафіку) | 1 |

### 2.9 Продуктивність і вага — 2,5

Адмінка збирається **в один чанк 545 KB** (Vite сам попереджає: >500 kB), платформа —
411 KB. Маршрути імпортуються статично; динамічний `import()` — **один**
(`RichTextField`) (**1** з 2). Для TWA на мобільному мережі це прямий час до першого
рендера.

### 2.10 Документація і процес — 8,8

README у всіх 6 воркспейсах (**2**); документи розділено так, що їх можна редагувати
(`PLAN` — стан і план, `LOG` — хронологія) (**2**); кожне число має джерело — план
звірено з кодом 12.09 (**2**); застарілих тверджень не лишилось — прибрано останнє
(`AGENTS.md` §5 описував `src/stores/app.store.ts`, якого більше немає) (**2**). Немає
**автоматичної** перевірки: жоден гейт не помічає, що документ розійшовся з кодом
(**0**). Саме тому попередні розбіжності («Family Box», 29 блоків, 3 148 рядків) жили
так довго.

---

