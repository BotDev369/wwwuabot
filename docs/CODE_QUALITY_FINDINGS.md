<!-- §3–§8: зрілість, знахідки, ціна кроків, межі -->
## 3. Де ми на шкалі зрілості

| Рівень | Стан |
|---|---|
| **L2 — керований** (правила діють для частини коду) | ✅ є |
| **L3 — визначений** (правила писані, гейти блокують деплой) | ✅ є — і це головний актив: CI, Prettier, lint, тести, `wrangler-env.test.ts` |
| **L4 — вимірюваний** (числові пороги в CI) | ❌ **немає жодного**: ні порогу покриття, ні бюджету бандла, ні перевірки ліміту рядків, ні перевірки «клас без CSS» |
| **L5 — оптимізуючий** | ❌ |

**Головний висновок:** проєкт виглядає як L3, але **ніщо не заважає йому котитися
назад у L2**, бо якість ніде не вимірюється автоматично. Саме тому 44 класи без CSS
і 15% мертвого коду прожили місяці: гейти перевіряють, що код *компілюється*, а не
що він *потрібен*.

---

## 4. Що знайдено конкретно (адреси, не оцінки)

### 4.1 Мертвий код — уже видалено (74 файли, 5 238 рядків)

| Острів | Файли | Рядки | Хто замінив |
|---|---:|---:|---|
| Легасі-`mydate` у платформі: `pages/mydate/*`, `shared/api/mydate.api.ts`, `stores/app.store.ts` | 13 | 1 909 | `packages/ui` блоки + Page Builder |
| Легасі-редактор блоків адмінки: `features/editor/blocks/**`, `store.ts`, `pages/editor/**` | 48 | 2 270 | `features/page-builder/` |
| Окремі сторінки: `DynamicPage`, `DynamicAdminPage`, `HomePage`, `ProfilePage` | 4 | 545 | `ScenarioPage` (усе зі сценаріїв) |
| Модерація-легасі, шими: `features/moderation/*`, `navItems.ts`, `SaveActionButtons.tsx`, `useSiteApi.ts`, `scenarios.api.ts`, 2 файли бота | 9 | 514 | спільний `SaveActionButtons`, `adminNav.store`, `sites`-API |

Той самий `RichTextField`, який динамічно тягне `BotRichConstructor`, лишився живий
(перевірено графом). Після видалення: `typecheck` чисто, lint 0/0, 182/182 тестів,
збірки обох оболонок ✅, вага бандла **не змінилась** (мертвий код і так
tree-shake-ився — борг був у репозиторії, не в рантаймі).

### 4.2 Те саме, ще не видалено

- **≥167 CSS-класів**, яких немає в коді (найбільші родини: `.usr-*`, `.scn-*`,
  `.tg-*`, `.bq-*`, `.pb-*`, `.rt-*`, `.phone-*`, `.date-input-*`, `.sys-*`);
- **44 класи `wb-block-*`** — це не зайвий CSS, а **відсутній** (§2.7).

### 4.3 Найризикованіше місце в репозиторії

`api-dev/src/services/sites.service.ts` — **777 рядків, 0 тестів**, і саме він
керує публікацією сайтів користувачів (`draft → pending → published → rejected`) і
правами власника. Помилка тут = чужий сайт опубліковано або свій недоступний.

---

## 5. Документація: окрема оцінка — 88%

Оцінюється **не обсяг**, а здатність документа не брехати:

| Перевірка | Факт | Бал |
|---|---|---:|
| Кожне число має джерело в коді | ✅ після звірки 12.09 (до неї — 12 вигаданих тверджень) | 2 |
| README на кожен воркспейс | 6 з 6 | 2 |
| Документ редагований цілком (не «перші 700 рядків») | ✅ `PLAN` 183 + `LOG` 1 100 замість одного на 1 090 | 2 |
| Кожна знайдена проблема має адресу (файл, рядок) | ✅ | 2 |
| Документація перевіряється автоматично | ❌ жодного лінк-чекера / звірки чисел у CI | 0 |
| Застарілих тверджень не лишилось | ✅ останнє (`AGENTS.md` §5 про `src/stores/app.store.ts`) прибрано в цій же зміні | 2 |

**88%** — це найвища оцінка в цьому аудіті. Вона чесна: документація тут справді
краща за середню по проєктах такого розміру, і слабке в ній рівно одне — **вона
не перевіряє себе сама**.

---

## 6. Як підняти відсоток: що скільки дає

| Крок | Що робить | Критерії | Приріст |
|---|---|---|---:|
| **1. Планка в CI** (ліміт рядків, «клас без CSS», бюджет бандла ≥600 KB, `no-explicit-any: error`) — **3 з 4 закрито 12.09.2026** (`check:quality`; лишився бюджет бандла) | перестає котитися назад у L2 | 1, 4, 7, 9 | **+9 п.п.** |
| **2. Тести на `sites`** (статусний автомат, права власника, каталог) | закриває найризикованіший модуль | 3 | **+7 п.п.** |
| **3. Блоки на `.wb-block-*`** (порції по 5–8, спереду — з хардкод-кольорами) | бренд і тема починають діяти на блоки | 7, 5 | **+8 п.п.** |
| **4. Розбити 4 файли >400** — **1 з 4 закрито 12.09.2026** (`sites.service.ts` 777 → 6 модулів, найбільший 178); лишились `ScenarioCardModal` 443, `UserProfileCard` 417, `UserEditModal` 401 | кристалевість | 1 | **+4 п.п.** |
| **5. Мобільний борг**: `@media (hover: none)`, 44px-таргети, прибрати `100vh`-голі | вага 15 — тут найдорожчі бали | 6 | **+6 п.п.** |
| **6. Мертвий CSS і `.usr-*`/`.scn-*` → `.wb-*`** | один набір класів | 7, 4 | **+5 п.п.** |
| **7. `SELECT *` на `users`, `npm audit` до 0 high** | прямі порушення власних правил | 8 | **+3 п.п.** |
| **8. Розбити бандли** (`lazy()` на маршрути, `manualChunks`) | час до першого рендера на телефоні | 9 | **+2 п.п.** |

**Стеля цього плану — 90–91%.** Решта ~9% — це те, чого план не дає: E2E і
контрактні тести, мобільне тестування на реальних пристроях, rate-limit під реальним
трафіком, Sentry в браузері, прод і моніторинг. Тобто **58% → ~90% реально**, а
100% у цьому репозиторії без прода не існує.

Порядок не випадковий: **пункт 1 — перший**, бо без вимірювань кожен наступний крок
можна непомітно відкотити. Це та сама причина, чому 15% мертвого коду жило так довго.

---

## 7. Межі цієї оцінки (читати перед цитуванням)

1. **Немає рантайм-вимірювань.** Продуктивність оцінена за вагою зібраних файлів, не
   за профілем у браузері чи на телефоні.
2. **Немає візуальної перевірки.** Прев'ю воркерів у середовищі аудиту немає, тому
   всі висновки про верстку — з CSS і розмітки, а не з екрана.
3. **Мертвий CSS — нижня оцінка.** Іменa, склеєні рядками (`wb-${x}`), статично
   невидимі; справжня цифра може бути більшою.
4. **Ваги — рішення, не істина.** 15 для мобільності обрано свідомо (TWA = телефони).
   Зміниш ваги — зміниться відсоток, і це нормально для будь-якої рубрики.
5. **Прода немає.** Тому «надійність» тут вимірюється тестами й гейтами, а не
   поведінкою під навантаженням.

---

## 8. Де що лежить

| Документ | Роль |
|---|---|
| `docs/CODE_QUALITY_AUDIT.md` (цей) | рубрика, оцінка, докази, план підняття балів |
| `docs/CONSOLIDATION_PLAN.md` | стан і план робіт (пункти з §6 цього файлу — там само) |
| `docs/CONSOLIDATION_LOG.md` | хронологія: що зроблено, деталі аудиту ризиків, журнал |
| `AGENTS.md` | правила, які й оцінювались (§3, §4, §7) |
