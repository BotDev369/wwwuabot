# Тема: схеми, шрифт і сторінки

Власник фактів про **вигляд, який вибирає людина**: схема (три кольори + шрифт), її бібліотека й
публічність, сторінки розділу «Тема» і друга смуга футера. Токени й правила вигляду —
[`DESIGN_SYSTEM.md`](./DESIGN_SYSTEM.md) (правила 12 і 19), таблиця `theme_schemes` —
[`DATA_MODEL.md`](./DATA_MODEL.md), публічність у стрічці — [`SPACE.md`](./SPACE.md).

## Схема — це набір, а не поточний вибір

**Схема = три кольори (фон / основний / акцент) + шрифт.** Бренду (Apple / Material) у схемі
**немає**: це характер продукту, а не вибір людини, і «поділитись схемою» не мусить нав'язувати
комусь характер.

**Дві різні речі живуть у двох різних місцях.** Що **зараз** на екрані — у пам'яті пристрою
(`localStorage`: `wwwuabot-colors`, `wwwuabot-font`), бо палітра мусить стояти на `<html>` до
першого рендера, а мережа відповіді не дає. **Бібліотека** схем — на сервері: її видно з будь-якого
пристрою, і саме її можна відкрити для інших.

**Поточний вибір ніколи не зберігається як схема сам собою.** «Схема» — це рішення людини назвати
вибір; локальний вибір без назви лишається вибором.

Застосування схеми — **одна** дія на кольори **й** шрифт (`applyThemeScheme`,
`@wwwuabot/shared/themes/apply`): два входи (список схем і готова палітра) не мають права
розійтися так, щоб десь колір застосувався, а шрифт лишився старий.

## Шрифт — безкоштовний набір із кирилицею

Родини — `THEME_FONTS` (`styles/fonts.ts`): тринадцять безкоштовних (Google Fonts, OFL) родин, у
кожній є **кирилиця** — інакше український текст мовчки випадав би в системну родину. Кожна несе
свою роль («без засічок», «із засічками», «акцидентні», «моноширинні»), тож список згруповано, а
підпис у ньому набрано **тим самим шрифтом** — назва родини нічого не каже про те, як вона виглядає.

- **У даних лежить `id`** (`inter`, `lora`), а стек із фолбеками рахує `fontStack()`. Записати стек
  означало б заморозити в даних рішення, яке ми самі міняємо в коді.
- **Порожній `id` — «як у стилі»**, законний стан: зняти свій шрифт можна, і він вертає брендову
  типографіку (`applyFont(null)` прибирає інлайнові змінні).
- **Файл поділений на дані й DOM** (`fonts.ts` / `font-dom.ts`): перевірка схеми живе на сервері, а
  один `document` затягнув би у збірку воркера всю клієнтську половину.
- Список вибору **завантажує всі родини лише собою** (`preloadFonts` / `releaseFontPreviews`): у
  застосунку тримається тільки вибрана.

## Тема — сторінки, а не поверхня

`/profile/theme` — хаб, у нього п'ять розділів зі **своїми адресами**: `style` (характер),
`presets` (готові трійки), `mine` (власні схеми), `public` (спільна бібліотека), `customize` (три
кольори, шрифт, назва, публічність). Склад і шляхи — `web-platform-dev/src/pages/themes/theme-sections.ts`,
і кожен розділ мусить мати маршрут (стереже `theme-sections.test.ts`).

**Чому не модалка.** У схеми є адреса, історія й «назад»: модалка не давала ні надіслати схему
посиланням, ні повернутися до неї. Модалка лишилась там, де вона доречна — у панелі (`ThemeSheet`:
там немає своїх сторінок).

**Друга смуга футера** (`SubBar`) веде між сторінками цього розділу: стиль, мої схеми, публічні,
налаштувати. Це не дубль хабу: хаб обирає, смуга перемикає. Кирпичик спільний — будь-який розділ,
що обросте сторінками, складається з нього, а не малює свою смугу.

## Публічність і Простір

Перемикач «Доступна публічно» у налаштуваннях робить схему видимою **всім**: вона з'являється в
розділі «Публічні схеми» й у вкладці **«Теми»** Простору. Обидва входи читають ту саму вибірку
(`is_public = 1`), тож другого списку не існує; видимість відбирає **запит до бази**, а заблоковані
люди в бібліотеку не потрапляють.

**Взяти чужу схему — один дотик**, і це не копія в базі: схема складається з трьох кольорів і
шрифту, тож «взяти» означає застосувати її собі. Змінювати й прибирати можна лише свою: кнопок під
чужою немає, бо сервер однаково відповів би 404 (`AGENTS.md` §7).

## Чого тут ще немає

- **Автора схеми не показано** — ні в спільній бібліотеці, ні в Просторі: публічний список не має
  другого правила «чиї дані показувати», а схема цінна собою, а не підписом;
- **прев'ю схеми — лише кольори** (три смуги): шрифт у картці названо словом, бо тринадцять
  завантажених родин у списку коштували б більше, ніж дають;
- **порядок і пошук у бібліотеці** не визначені: схеми йдуть свіжими першими.

## Файли

| Що | Де |
|---|---|
| Набір шрифтів і правила | `packages/shared/src/styles/fonts.ts`, `font-dom.ts` |
| Схема: типи, перевірка, клієнт, застосування | `packages/shared/src/themes/*` |
| Сервіс і контролер | `api-dev/src/services/themes.service.ts`, `api-dev/src/controllers/themes.controller.ts` |
| Сторінки теми, смуга, хуки | `web-platform-dev/src/pages/themes/*`, `web-platform-dev/src/layout/ThemeSubBar.tsx` |
| Смуга як кирпичик | `packages/ui/src/nav/SubBar.tsx`, `packages/shared/src/styles/subbar.css` |
