# План консолідації: одна кодова база + дві тонкі оболонки

**Дата:** 12.09.2026 · **звірено з кодом:** 14.09.2026 (кожне число — з `wc -l` і пошуку,
не з оцінок). **Статус:** безпека, дедуплікація й «один дизайн» крок 1–2 закриті;
актуальний план робіт — **§3**.

> **Історія й деталі закритих робіт — [`HISTORY.md`](./HISTORY.md)** (до 13.09.2026 це були
> 30 файлів: полотно + журнал на кожну роботу). Нумерація збережена: §1 (стан оболонок),
> §2 (правило межі), §3.1–§3.5 (дублювання), §4.1–§4.5 (Page Engine, мобільний, діалоги,
> клас без стилю), §5.1–§5.8 (аудит ризиків), §9 (журнал виконаного). Посилання виду
> «LOG §5.4» у коді й документах ведуть саме туди.

---

## 0. Стан на 14.09.2026 (виміряно)

| Метрика | Значення |
|---|---|
| Код | 272 файли `.ts`/`.tsx` у `src/` шести воркспейсів без тестів, **26 836** рядків; разом із тестами — **306 / 30 567** (код не зменшується від рефакторингу, зменшується **найбільший** файл) |
| Оболонки | `web-platform-dev` 632 + `web-admin-dev` 9 465 = **10 097** (платформа була 2 297 — редактор сайтів зник, але адмінка росте: картки сценаріїв, Page Builder, портал) |
| CSS | `packages/shared/src/styles/` **4 282** (10 файлів) · платформа **327** (було 1 223) · адмінка **1 993** (було 2 473 + `profile.css` 84). Разом **6 602** — було 7 366 |
| Спільний каркас | **Було 0** спільних за назвою класів між оболонками — тепер обидві складаються з кирпичиків `.wb-*` з `app-chrome.css: 198 класів у спільному коді + 207 у розмітці оболонок, усі мають правила |
| Тести | **312** у 34 файлах (3 731 рядків); гейтять CI (червоний тест блокує деплой). Динаміка: +33 на профіль і ім'я на платформі (14.09), +7 на нижній футер (14.09), +31 на адресу й портальний CRUD (14.09), +14 на реєстр D1, +24 на модель контенту — і −78 разом із доменом `sites` |
| Середовища | усі 4 воркери — дев (`ENVIRONMENT = "dev"`); **прода немає і він ніколи не деплоївся** |
| CI | зелений run `34763232343` (13.09) — `checks` + **4 деплої** |
| Планка | `npm run check:quality` — 4 правила (ліміт рядків, нативні діалоги, голий `100vh`, емодзі в UI), 271 файл, найбільший 367 рядків; **файлів > 400 немає** — леджер боргу порожній (лишився тільки `icons.tsx` як дані) |
| Документи | **14** (міряє сам гейт) — було 44: 30 файлів історії зведено в один `docs/HISTORY.md`, два документи аудиту — в один `docs/CODE_QUALITY.md`. Монолітів > 200 рядків немає, крім `AGENTS.md` (400 — стеля: він не ділиться) і цього плану. `npm run check:docs`: розмір, мертві посилання, «§N» з коду |
| Схема D1 | **4** таблиці у реєстрі (`users`, `settings`, `scenarios`, `mydate_analysis`) — рівно те, що стоїть на дев-базі; `users` отримав `platform_username` (ім'я на платформі) і `telegram_json` — [«дві різні імена»](./DATA_MODEL.md); поза реєстром жодна не створюється — стереже `npm run check:db` (крок `D1 schema`). Сховище контенту — **одне** (`scenarios`: рядок = сторінка вебу + її подання в боті); таблицю `pages` і міграцію до неї скасовано 13.09.2026 як п'яте сховище того самого ([`CONTENT_MODEL.md`](./CONTENT_MODEL.md)) |

Найбільші файли (ліміт кристалевості — 200 рядків, 400 — критично):

| Файл | Рядків | Стан |
|---|---:|---|
| `packages/ui/src/store.ts` | 367 | 🎯 пункт 5 плану |
| `web-admin-dev/src/pages/users/UsersTable.tsx` | 359 | 🎯 пункт 5 плану |
| `api-dev/src/services/users.service.ts` | 349 | 🎯 пункт 5 плану |
| `packages/shared/src/components/icons.tsx` | 408 | ➖ дані, не логіка — ліміт не про нього |

**Понад 400 рядків немає жодного** (14.09.2026): три файли-«простині» поділено (§3 пункт 5).

---

## 1. Правила, які з'явились із цієї роботи

Повні формулювання — в `AGENTS.md` (він авторитетний); тут лише перелік, щоб знати, що шукати:

| Правило | Де живе |
|---|---|
| Один дизайн, дві оболонки: візуально однакові, різниця лише в логіці й правах | `AGENTS.md` §3 |
| Мобільний — пріоритет №1 (`100dvh`, `var(--safe-*)`, тап-таргети ≥44px, `@media (hover: none)`) | `AGENTS.md` §3 |
| Клас без правила — баг; CSS спільного коду живе в `packages/shared/src/styles/` | `AGENTS.md` §3 · `check:css` |
| Нативні `alert`/`confirm`/`prompt` заборонені — на iOS їх немає; тільки `useDialog()` | `AGENTS.md` §4 · `check:quality` |
| «Це прод?» вирішує `isProduction()`, а `ENVIRONMENT` — ім'я воркера | `AGENTS.md` §1 · `wrangler-env.test.ts` |
| Право власника не можна перевіряти за одним сегментом URL (`:slug` + чужий `:pid` = IDOR) | `AGENTS.md` §7 |

---

## 2. Що закрито

Деталі кожної закритої роботи — у журналі: покажчик «старий § → файл» у
[`HISTORY.md`](./HISTORY.md) — леджер рішень §0–§8 і хроніка §9.
Живий список відкритого — §3 нижче.

---

## 3. План робіт (за спаданням користі)

**Правило:** один пункт = один коміт = окремий деплой. Кожен пункт можна відкотити
поодинці, бо всі вони зворотносумісні.

**Порядок і ціна кожного пункту в балах — `docs/CODE_QUALITY.md` §5** (той же
список, але з оцінкою «скільки відсотків якості дає крок»). Першим там стоїть пункт 0:
**планка в CI**, бо без вимірювань кожен наступний крок можна непомітно відкотити.

> ⚠️ **13.09.2026: пункти 1, 2 і 8 закрито видаленням.** Власник прибрав таблиці `sites`,
> `site_pages`, `templates` — разом із ними зник увесь код навколо (6 модулів
> `services/sites/*`, 4 контролери, ~15 маршрутів, редактор сайтів платформи, сторінки
> модерації й шаблонів адмінки, `SiteRenderer`, 9 документів специфікації).

**1. Тести на `sites` — найбільша діра в покритті (історія).**
> Домену більше не існує, але пункт лишається повчальним: **68 тестів** на шість модулів
> знайшли справжній дефект — IDOR на `:pid` (свій `slug` + чужий `pageId` = запис у чужу
> сторінку); виправлено, закрито регресією. Урок живий у `AGENTS.md` §7, деталі — `HISTORY.md` §9.

**2. Розбити `sites.service.ts` (777) — історія.**
> Закрито поділом 12.09.2026 (`sites.*` → 6 модулів, найбільший 178) і видаленням 13.09.

> ~~**`sites.service.ts` (777)**~~ — **закрито 12.09.2026**: поділено на `services/sites/*`
> (777 → 178; перенос механічний — 26 експортів до і 26 після, тіла функцій знайдені дослівно).
> **`site-templates.ts` (623)** дробити не довелось: його видалено разом із доменом 13.09.2026.

**0. Планка в CI — пороги замість добрих намірів (`CODE_QUALITY.md` §5, крок 1).**
Ліміт рядків на файл, бюджет бандла, `no-explicit-any: error`, поріг покриття для нових
модулів. Без цього пункти 1–8 повертаються назад: саме тому 44 класи без CSS і 15%
мертвого коду прожили місяці — гейти перевіряють, що код компілюється, а не що він потрібен.

> ~~Перевірка «клас без CSS»~~ — **закрито 12.09.2026**: `npm run check:css` у джобі
> `checks` (той же гейт на PR).
>
> ~~Ліміт рядків на файл, `no-explicit-any: error`, заборона нативних діалогів, голого
> `100vh` і емодзі в UI~~ — **закрито 12.09.2026**: `npm run check:quality`, той самий крок
> у `checks`; деталі — `docs/QUALITY_GATE.md`. Гейт знайшов 16 емодзі й виправив два
> числа в аудиті (`CODE_QUALITY.md` §2).
>
> **Відкрито:** бюджет бандла (≥600 KB) і поріг покриття для нових модулів — обидва
> потребують змін у пайплайні (`vite build` у `checks`) і мають вноситись окремим кроком.

**3. Крок 3 «одного дизайну» (§4.3).**
- **44 класи `wb-block-*` не мають жодного правила** — блоки стилізовані inline
  (`style={{ … }}`: 192 об'єкти, 30 хардкод-hex, 8 `rgba()`). Через це `data-brand`
  і `data-theme` до блоків **не доходять**, а `index.css` оболонок на них не впливає.
  **Дія:** порціями по 5–8 блоків переносити inline → `.wb-block-*` у `components.css`,
  починаючи з тих, де є хардкод-кольори.
- ~~**Дві реалізації тієї самої модалки дати**~~ — **закрито 12.09**: легасі-версію
  платформи видалено разом з островом `pages/mydate/`, лишилась `packages/ui` (193 рядки).
- **Мертвий CSS** — **почищено 12.09.2026 з обох боків**: 164 → 101 клас, якого немає в
  `src/` навіть як підрядок (каркас оболонок, `profile.css`, острів `mydate`-класів).
  Лишок — брендові легасі (`html[data-brand] .btn`, `.card`, `.kb-*`), яких немає в розмітці.
- ~~**Емодзі в UI**~~ — **закрито 12.09.2026**: 16 справжніх емодзі в 7 файлах замінено
  на `<Icon />`, і тепер новий емодзі не пройде CI (`check:quality`).
- ~~**`↑ ↓ ✕` текстом** замість `<Icon />` у редакторах блоків адмінки~~ — **закрито 14.09.2026**:
  `▲ ▼ ✕ ↑ ↓` → `icons` зі shared і кирпичики `.wb-move-btn` / `.wb-close-btn`; мертвий
  `.block-actions` прибрано. Символи гейт не ловив — це була ручна робота.
- **`.usr-*` / `.scn-*` в адмінці дублюють `.wb-data-table` / `.wb-modal-*`** — найдорожчий
  пункт, робити тільки разом із візуальною перевіркою.

**4. Поділ `web-admin-dev/src/index.css` (1 995 рядків після кроку 1 «одного дизайну»).**
Найбільший CSS-файл проєкту; у `packages/shared` разом — 4 008. Поділ на `styles/*.css` зі
спільним `index.css`-імпортом: сценарії, редактор блоків, page builder, таблиці.
Робити разом із візуальною перевіркою.

**5. Решта великих файлів.**
Понад 400 рядків більше немає: `ScenarioCardModal.tsx` (443), `UserEditModal.tsx` (401) і
`UserProfileCard.tsx` (417) поділено 14.09.2026, леджер `quality-baseline.mjs` тепер порожній.
> ~~Три файли > 400~~ — **закрито 14.09.2026**: стан і збереження пішли в хуки
> (`useScenarioCard`, `useScenarioJsonEditor`, `useUserEdit`), розмітка — у підкомпоненти;
> `UserProfileCard` став каркасом над `components/user-profile/*`.

Далі — «червоні прапорці» понад 200 рядків (**29 файлів** на 14.09.2026, найбільший 367).
Найдешевші: `packages/ui/src/store.ts` (367), `UsersTable.tsx` (359),
`api-dev/src/services/users.service.ts` (349). Один файл зі списку — **дані, а не логіка**:
`icons.tsx` (408); йому ліміт рядків не потрібен, дробити його навпіл шкідливо.

**6. Дрібне з §3.5, що ще відкрите.**
Alias `@/*` в адмінці. Злиття `profile.css` у спільний шар — **закрито 12.09.2026**
(сходило до `.wb-profile*` в `app-chrome.css`).
**`INSERT INTO users … '...'`** у `my-dates.controller.ts` — api-dev створює рядок
користувача з фальшивими іменами, хоч таблиця належить `bot-dev` (`DATA_MODEL.md`).

**7. Спостережуваність у браузері (окремий крок).**
Sentry для `web-platform-dev` / `web-admin-dev` потребує `VITE_`-DSN на етапі збірки.
Зараз помилки UI не видно ніде, крім консолі користувача.

**8. Дві таблиці сценаріїв — закрито** 13.09.2026: схеми звели в реєстр, а потім власник
підтвердив, що друга таблиця (`scenarios-admin`) була тестом. Її видалено разом із кодом —
і питання про поле `scope` зникло саме собою. Того ж дня прибрано й другу лінію контенту
(`sites` + `site_pages` + `templates`), а потім і «цільову» `pages` разом із міграцією:
сховище одне — `scenarios`.

**Не входить у план:** Cloudflare Access і власні домени (`admin.` / `app.`) — це задум, а не зроблене; зараз захист адмінки — cookie + адмін-гейт.

---

## 4. Рішення, які потрібні від власника

| # | Питання | Стан |
|---|---|---|
| 1 | ~~**Дві таблиці сценаріїв** — лишаємо чи мігруємо в одну з полем `scope`?~~ | ✅ закрито 13.09.2026: другу таблицю видалено (тест), другу лінію контенту (`sites` + `site_pages` + `templates`) — теж, а таблицю `pages` і міграцію скасовано. Джерело одне — `scenarios` |
| 2 | **`/api/mydate/*`** — публічний *запис* у D1/KV: `initData` (простіше) чи Cloudflare Rate Limiting (дешевше за CPU)? | відкладено до реального трафіку (§5.6) |
| 3 | **Cloudflare Access + домени** для адмінки — робимо чи лишаємо cookie+`*.workers.dev`? | відкрито (§8; `docs/PAGE_ENGINE_ARCHITECTURE.md` §2) |
| 4 | **Sentry у браузерних застосунках** — потрібен секрет GitHub Actions + `VITE_`-DSN | чекає рішення (§3 пункт 7) |
| 5 | Правило межі з §2 — прийнято й застосовано в коді | ✅ закрито |

---

## 5. Чого цей план НЕ дасть (чесно)

| | |
|---|---|
| **Уже отримано** | CSS 7 366 → 6 330 (−1 036: спільні кирпичики каркаса + мертвий CSS обох оболонок), один діалог замість 33 зламаних на iOS викликів, і мінус цілий домен `sites` (разом із дубль-таблицею `pages` — 8 210 рядків видалено одним комітом) |
| **Що НЕ зміниться** | модель деплою, роутинг, бізнес-логіка, кількість воркерів, відсутність прода |
| **Чого не буде від рефакторингу** | тестів, моніторингу браузера, захисту адмінки Cloudflare Access — це окремі роботи, а не наслідок чистоти коду |

**Головний ефект — не рядки.** Зникає потреба правити одне й те саме у двох застосунках і
зникає клас багів «розсинхрон між оболонками» (приклади: `.block-card:hover` на мертвому
класі → кнопки блока були невидимі назавжди; нативний `prompt` → «Додати сторінку» на iOS
не робила нічого).

Оцінка «−70..75%» з `docs/PAGE_ENGINE_ARCHITECTURE.md` §6 стосується **окремих файлів**, а не
репозиторію: жоден рефакторинг не прибирає продукт (поточні числа — §0). Реалістично
скоротити можна кілька відсотків, і то переважно там, де є справжнє дублювання (`.usr-*` / `.scn-*`).

---

## 6. Де що лежить

Покажчик усієї документації — [`docs/README.md`](./README.md): один документ = одна
тема = один власник факту. Куди писати нове — там же, таблиця «Куди писати нове».

