# План консолідації двох застосунків

**Дата:** 11.09.2026
**Обсяг:** `web-platform-dev` (6 271 рядок TS/TSX) + `web-admin-dev` (13 773) = 20 044 рядки, 47% усього репозиторію
**Статус:** у виконанні. Закрито: §5.3 (ідентичність), §3.1 (HMAC сесії), §3.4 (контролери
сценаріїв), §5.1 (`@wwwuabot/ui`), §5.5 (застарілі числа в доках), §5.2 (D1-бінд­инг адмінки + `AGENTS.md`).
Свідомо відхилено: §3.2 (див. нижче). Наступне: §3.5 (дрібне) → §4.2 (index.css адмінки).
§3.3 (`useMyDates`) закрито 11.09.2026.
Моніторинг (§5.8): код готовий — `/health/deep` + Workers Logs; UptimeRobot підключає власник.

**Остання перевірка (12.09.2026):** `npm test` — 162/162 ✅ · `npm run typecheck` — чисто на
всіх 6 воркспейсах ✅ · `npm run lint` — 0 errors ✅ · CI run `34662385702` — усі 6 джобів ✅.
Живе: `/api/scenario/%` віддає **400** (було 500), адмін-префікси — 401, публічні — 200.
Секрет `SENTRY_DSN` додано в `api-dev` і `bot-dev` власником акаунта. Деталі — §9.

---

## 0. Що тут означає «консолідація»

Це **не** злиття двох воркерів в один. Рішення про роздільні воркери вже затверджене в
[PAGE_ENGINE_ARCHITECTURE.md](./PAGE_ENGINE_ARCHITECTURE.md) (09.09.2026) і має обґрунтування,
яке я перевірив і вважаю слушним:

1. **Zero bundle leakage** — адмінський JS фізично не потрапляє в бандл користувача.
2. **Same-Origin Policy** — адмінська cookie не читається з публічного домену.
3. **Cloudflare Access** — edge-захист адмінки не існує, якщо об'єднати.
4. **Least privilege** — адмінка має Service Bindings, яких публічний застосунок не має.

> **Раніше я пропонував об'єднати обидва застосунки в один білд і оцінював це в −5..7k рядків.
> Знімаю цю пораду.** Аргумент про витік адмінського коду в публічний бандл реальний, а
> TWA-застосунок вантажиться мільйонам користувачів. Роздільні origin'и тут — правильно.

**Отже, консолідація = одна кодова база + дві тонкі оболонки.**
Мета — прибрати дублювання *між* оболонками, не змінюючи модель деплою.

---

## 1. Стан фактів: оболонки вже майже ідентичні

Аудит показав, що різниці значно менше, ніж можна очікувати. Це добре — означає, що
консолідація це переважно видалення, а не переписування.

### Вже однакове (перевірено)

| Що | web-platform | web-admin |
|---|---|---|
| `src/App.tsx` | **байт-в-байт ідентичний** (`AuthGate` + `RouterProvider`) | те саме |
| `fixHtml`-плагін у `vite.config.ts` | ідентичний код | ідентичний код |
| React / react-router / zustand | 19.2.7 / 7.18.2 / 5.0.14 | **ті самі версії** |
| vite / tailwind / wrangler / typescript | 8.1.1 / 4.3.3 / 4.112 / ~6.0.2 | **ті самі версії** |
| `tsconfig.app.json` | `strict: true`, `noUnusedLocals` | те саме (різниця: platform має `@/*`) |
| CSS-фундамент | `@import "@wwwuabot/shared/styles/index.css"` + `tailwindcss` | те саме, той самий шрифт |
| `worker.ts` → `fixAssetHeaders()` | ідентична функція | ідентична функція |
| SPA через один воркер + ASSETS | так | так |

### Реально різне (і мусить таким лишитись)

| Вісь | web-platform | web-admin |
|---|---|---|
| Контекст входу | Telegram WebApp SDK | браузер |
| Auth | `X-Telegram-Init-Data` (HMAC на `BOT_TOKEN`) | cookie `admin_session` (HMAC на `ADMIN_SECRET`) |
| Роутинг | catch-all → `ScenarioPage` (сценарії з БД) | статичні роути: scenarios, users, sites, templates |
| D1-біндинг | немає | **немає** (зайвий бінд­инг прибрано 11.09.2026 — §5.2) |
| ASSETS-корінь | `./dist` | `./dist/client` |
| SPA-fallback | декларативний (`not_found_handling`) | ручний у `worker.ts` |

---

## 2. Правило межі (головне рішення)

Усе інше випливає з одного правила. Пропоную зафіксувати його в `AGENTS.md`:

| Шар | Можна ділити? | Куди |
|---|---|---|
| Типи, утиліти, токени, CSS | ✅ так | `packages/shared` |
| Рендеринг блоків і сторінок (`PageRenderer`) | ✅ так | `packages/ui` |
| Транспорт API (`apiFetch`) | ✅ так | `packages/shared` |
| Перевірка сесії (HMAC, cookie, initData) | ✅ так, як **чиста функція** | `packages/shared` |
| AuthGate (оболонка рішення) | ❌ ні | окремо в кожному застосунку |
| Роутер, `worker.ts`, `wrangler.toml` | ❌ ні | окремо |
| Редакторські UI адмінки | ❌ ні | `web-admin-dev` |

**Ключове розмежування:** *логіка перевірки* — спільна; *рішення, що робити при провалі* — своє
в кожного (TWA показує «відкрийте в Telegram», адмінка — `LoginScreen`).

---

## 3. Етап 1 — прибрати справжнє дублювання (механічно, безпечно)

Усе нижче — перевірені знахідки, не припущення. Кожен пункт зворотносумісний: поведінка не змінюється.

### 3.1. HMAC-перевірка сесії задубльована в двох воркерах ⚠️ найважливіше

Одна й та сама логіка (`getKey`, `hexToBytes`, `verifyToken`, `parseCookies`,
`isAuthenticated`) написана **двічі**:

- `api-dev/src/controllers/auth.controller.ts` (рядки 30–113)
- `web-admin-dev/src/worker.ts` (рядки 28–95)

Це не косметика: якщо хтось змінить формат токена в одному місці, авторизація зламається
в іншому — і зламається тихо.

**Дія:** винести в `packages/shared/src/security/session.ts` як чисті функції
(`signToken`, `verifyToken`, `parseCookies`, `isAuthenticated`). Обидва воркери імпортують.
**Ефект:** −90 рядків, зникає клас багів «розсинхрон auth».

### 3.2. Два обгортання `apiFetch` з різною поведінкою

- `web-platform-dev/src/shared/api/client.ts` (16 рядків)
- `web-admin-dev/src/shared/api/client.ts` (21 рядок)

Різниця рівно дві: `credentials: "same-origin"` і `onUnauthorized`. Обидва вже
викликають спільний `packages/shared/src/api/client.ts`, який **уже підтримує обидва
параметри**.

**Дія:** один фабричний хелпер у shared: `createApiClient({ credentials, onUnauthorized })`.
**Ефект:** −30 рядків, одна точка зміни транспорту.

> **ВІДХИЛЕНО 11.09.2026 — свідомо.** Транспорт уже єдиний
> (`packages/shared/src/api/client.ts`), а різниця між обгортками — це політика
> застосунку (заголовки ідентичності в TWA, cookie+reload в адмінці), яку §2
> прямо дозволяє тримати окремо. Фабрика додала б рівень непрямості заради
> ~10 рядків. Замість фабрики обгортки лишено тонкими та явними. Рішення можна
> переглянути, якщо з'явиться третій споживач транспорту.

### 3.3. `useMyDates` реалізований двічі

- `web-platform-dev/src/pages/mydate/useMyDates.ts` — **253 рядки**
- `packages/ui/src/blocks/my-dates-table/useMyDates.ts` — **245 рядків**

Це два різні хуки для однієї задачі (платформена сторінка vs блок рендерера).
Сумарно 498 рядків там, де потрібно ~260.

**Дія:** один канонічний хук у `packages/ui/src/blocks/my-dates-table/` з опціями
(`showSearch`, `showTypeFilter`, `showBulkActions` — вони вже є в UI-версії), платформена
сторінка його імпортує.
**Ефект:** −240 рядків, і фікси робляться один раз.

> **ЗАКРИТО 11.09.2026.** Канонічний хук — `packages/ui/src/blocks/my-dates-table/`,
> платформеного файлу більше немає (сторінка імпортує з `@wwwuabot/ui/blocks/my-dates-table`).
> Чотири невикористані опції (`showSearch`, `showTypeFilter`, `showBulkActions`,
> `showCreateButton`) видалено — хук їх ніколи не читав; блок отримує їх зі своїх `props`.
>
> Розбитий за правилом кристалевості (найбільший файл — 119 рядків):
>
> | Файл | Рядків | Роль |
> |---|---|---|
> | `useMyDates.ts` | 119 | завантаження, CRUD, композиція |
> | `filter-sort.ts` | 111 | чисті функції: теги, фільтри, сортування |
> | `useDateFilters.ts` | 98 | стан сортування/фільтрів + похідні списки |
> | `useDateSelection.ts` | 42 | вибір рядків |
> | `useDateModal.ts` | 41 | стан модалки (потрібен блоку) |
> | `useMyDates.types.ts` | 36 | типи хука |
>
> **Економія рядків — не −240, а ≈ −50 (498 → 447).** Хук віддав перетворення чистим
> функціям, але лишив **обидва** набори можливостей (модалка блока + семантичні дії
> `setSort`/`toggleColumnFilter`/`clearSelection` для сторінки) і розділився на шість
> файлів із докблоками. Реальний виграш інший: **одна реалізація замість двох** — фікс
> робиться один раз, і сторінка більше не тримає власний `useEffect` із `eslint-disable`
> (завантаження — через опцію `autoFetch`, оновлення після save/delete — `refreshAfterMutation`).
>
> **Побічно знайдено і виправлено дефект:** сортування порівнювало рядки код-поінтами,
> тому «і» (U+0456) ставало **після** «я» — український список виглядав перемішаним.
> Тепер `localeCompare("uk")`. Саме тест на ці чисті функції його й виявив.
>
> **Тести:** `filter-sort.test.ts` (8) — фільтри, теги, сортування за датою/назвою,
> незмінність вхідного масиву.
>
> **Лишилось:** `MyDatesPage.tsx` — 381 рядок (було 396), усе ще червоний прапорець.
> Розбити на таблицю + панель фільтрів — окремим кроком (§3.5).

### 3.4. Контролери сценаріїв — той самий файл двічі

- `api-dev/src/controllers/scenarios-admin.controller.ts` — 304 рядки (таблиця `scenarios-admin`)
- `api-dev/src/controllers/scenarios-portal.controller.ts` — 274 рядки (таблиця `scenarios`)

Порівняння diff'ом: відрізняються **тільки** константою `TABLE` і докблоком.
Це наслідок рішення мати дві таблиці з однаковою схемою.

**Дія:** один контролер `scenarios.controller.factory.ts`, параметризований назвою таблиці.
Обидва наявні маршрути лишаються незмінними.
**Ефект:** −280 рядків.

> **ЗАКРИТО 11.09.2026.** Фабрика `createScenariosController({ table, ensureTable })`
> + спільний `ensureScenariosTable()`. `scenarios-admin.controller.ts` і
> `scenarios-portal.controller.ts` тепер по ~30 рядків конфігурації; роутер не
> змінювався. Заразом прибрано розходження в `handleUpdate`: `updated_at` більше
> не приймається з тіла запиту — він завжди ставиться сервером.

> **Окремо на затвердження:** дві таблиці (`scenarios` і `scenarios-admin`) з ідентичною
> схемою — це корінь цього дублювання. Одна таблиця + поле `scope` (`portal` | `admin`)
> прибрало б і дублювання в API-клієнті, і два набори міграцій. Але це міграція даних,
> тому — окреме рішення, не в межах Етапу 1.

### 3.5. Дрібне

| Що | Дія | Ефект |
|---|---|---|
| `app.store.ts` є і в `packages/shared/src/stores/`, і в `web-platform-dev/src/stores/` | лишити спільний | −7 |
| `web-admin-dev/src/profile.css` (84) | злити в спільні стилі | −84 |
| `web-admin` не має alias `@/*` (platform має) | додати в `tsconfig` + `vite.config` | консистентність |

**Разом Етап 1: ≈ −730 рядків і, головне, знято 4 класи «треба правити в двох місцях».**

---

## 4. Етап 2 — завершити затверджений Page Engine

Рішення з `PAGE_ENGINE_ARCHITECTURE.md` §6 виконане **частково**. Факти:

| Файл | Було (док) | Зараз | Стан |
|---|---:|---:|---|
| `web-admin-dev/src/features/page-builder/PageBuilderInline.tsx` | 572 | **206** | ✅ зроблено |
| `web-platform-dev/src/pages/mydate/MyDatesPage.tsx` | 413 | **396** | ⏳ майже не рухався |
| `web-admin-dev/src/pages/scenarios/ScenarioCardModal.tsx` | — | 403 | ⏳ |
| `packages/shared/src/components/icons.tsx` | — | 559 | ⏳ |
| `api-dev/src/services/sites.service.ts` | — | **868** | ⏳ |

**Дія:** продовжити за тим самим патерном — сторінки стають конфігурацією для `PageRenderer`,
а не версткою. Порядок за спаданням вигоди: `MyDatesPage` → `ScenarioCardModal` → решта.

### 4.1. Два набори блоків — уточнити межу

Зараз існують:

- `packages/ui/src/blocks/` — **40 рендер-компонентів** (для показу)
- `web-admin-dev/src/features/editor/blocks/` — **12 категорій** (для редагування)
- `packages/shared/src/constants/block-definitions/` — **733 рядки**, 29 типів (схема)

Три шари — це **правильно** (схема / рендер / редактор — різні задачі), не дублювання.
Але лишається питання: чи всі 29 типів блоків реально використовуються в контенті?

**Дія:** перевірити `page_data` у БД, які `type` реально зустрічаються. Невикористані —
не видаляти, а позначити як `experimental` і не показувати в галереї.

### 4.2. `web-admin-dev/src/index.css` — 3 148 рядків

Обидва застосунки вже імпортують спільний `@wwwuabot/shared/styles/index.css`, але адмінка
додає поверх ще 3 148 рядків (включно з власним `reset`, `body`, `html`). Частина
повторює спільне, частина — справді адмінська (таблиці, модалки, page builder).

**Дія:** прибрати з адмінки те, що вже є в shared (reset, body, типографіка, кнопки `.wb-*`),
лишити лише унікальне. Порівнювати покроково, візуально перевіряючи.
**Ефект:** оціночно −1 500 рядків. **Це найбільша механічна економія у всьому плані.**

**Разом Етап 2: ≈ −2 500..3 500 рядків.**

---

## 5. Етап 3 — ризики, знайдені під час аудиту

Це не рефакторинг. Це речі, які я знайшов, і вони можуть вистрілити.

### 5.1. ⚠️ `@wwwuabot/ui` не оголошений у `web-platform-dev/package.json`

Платформа імпортує `@wwwuabot/ui` у 4 файлах (`ScenarioPage`, `DynamicPage`,
`SiteEditorPage`, `SiteViewPage`), але в `dependencies` є тільки `@wwwuabot/shared`.

Зараз це працює **випадково** — через hoisting `node_modules` у npm workspaces.
Доказ, що залежність реальна: CI у `.github/workflows/deploy.yml` включає
`packages/ui/**` у фільтр для `web`.

**Дія:** додати `"@wwwuabot/ui": "*"` у `dependencies`. Один рядок, знімає ризик
«білд зламався після оновлення hoisting'у».

> **ЗАКРИТО 11.09.2026.** Залежність оголошена в `web-platform-dev/package.json`.
> Заразом прибрано залишковий службовий коментар з `web-platform-dev/src/worker.ts`.

### 5.2. `AGENTS.md` описує архітектуру, якої вже немає

`AGENTS.md` §1 стверджує: «`bot` і `web-admin` пишуть у D1 напряму».
Насправді:

- `web-admin-dev/wrangler.toml` справді має біндинг `[[d1_databases]] DB`
- але `env.DB` **не використовується ніде** в коді адмінки — `worker.ts` проксує все
  через `/api/*` у `api-dev`
- тобто біндинг зайвий, а документація вводить в оману

**Дія:** прибрати невикористаний D1-біндинг зі `wrangler.toml` адмінки (або підтвердити,
що він потрібен) і поправити `AGENTS.md` §1.

> **ЗАКРИТО 11.09.2026.** Біндинг `[[d1_databases]] DB` прибрано з
> `web-admin-dev/wrangler.toml` (у коді не використовувався — перевірено `grep env.DB`).
> `AGENTS.md` §1 виправлено: «`bot` пише в D1 напряму; `web` і `web-admin` — тонкі
> оболонки, жоден із них не має прямого доступу до D1». Заразом поправлено
> `web-admin-dev/README.md` і `docs/PAGE_ENGINE_ARCHITECTURE.md` §2.4, де лишалося
> твердження про прямі Service Bindings до D1.

### 5.3. ⛔ Ідентичності користувача не перевіряють — ЗНАЙДЕНО Й ПІДТВЕРДЖЕНО 11.09.2026

Це найважливіша знахідка аудиту. Авторизації користувача фактично немає.

**а) 16 хендлерів сайтів беруть особистість із непідписаної cookie.**

`getUserIdFromRequest()` — три ідентичні копії:
`sites.controller.ts:39`, `site-pages.controller.ts:34`, `templates.controller.ts:32`:

```ts
/** Витягує userId з cookie (HMAC auth). */
function getUserIdFromRequest(request: Request): number | null {
  const match = (request.headers.get("Cookie") ?? "").match(/user_id=(\d+)/);
  if (match) return parseInt(match[1], 10);
  return null;
}
```

Коментар каже «HMAC auth», але HMAC тут немає — це просто читання числа з cookie.

**б) Цю cookie ніхто і ніколи не ставить.** У всьому репозиторії єдиний `Set-Cookie` —
це `admin_session` в `auth.controller.ts`. Наслідки два, і обидва погані:

- **Зараз:** усі 16 хендлерів завжди повертають 401 — **фіча «сайти» мертва.**
- **Якщо cookie колись з'явиться:** будь-хто виставляє собі `user_id=1` і керує
  чужими сайтами, сторінками й шаблонами.

**в) `my-dates` приймає непідписаний `X-Telegram-User-Id`.**
`my-dates.controller.ts:114-134`: якщо `X-Telegram-Init-Data` є — перевіряється підпис;
якщо немає — береться голий числовий заголовок і код сам це логує:
`"unsigned X-Telegram-User-Id accepted"`. Тобто достатньо не надіслати initData, щоб
читати й писати чужі дати.

**г) `/api/user/profile?user_id=N` — публічний, без авторизації.**
`users.controller.ts:222-250` бере `user_id` з query-параметра і віддає `role`, `tariff`,
`status`, `discount`, `permissions`. Тобто будь-хто дізнається тариф і права будь-якого
користувача.

**д) `AuthGate` платформи — це перевірка браузера, не сервера.**
`web-platform-dev/src/app/AuthGate.tsx` робить лише
`!!window.Telegram?.WebApp?.initDataUnsafe?.user?.id`.

**е) Тестів на auth — нуль.**

**Дія:** одна довірена ідентичність — **тільки підписаний `initData`** (`verifyInitData`).
Відхиляти `X-Telegram-User-Id`, прибрати непідписану cookie, `/api/user/profile` має брати
`user_id` з перевіреного підпису, а не з query. Разом із сервером правиться клієнт
(надсилати `initData` у кожному запиті) — інакше сайти лишаться мертвими.

> **ЗАКРИТО 11.09.2026.** Єдина точка правди — `api-dev/src/shared/identity.ts`:
> `user_id` береться ТІЛЬКИ з підписаного `initData`. Непідписану cookie, голий
> `X-Telegram-User-Id` і `?user_id=` в `/api/user/profile` прибрано. Регресійні
> тести (`api-dev/src/shared/identity.test.ts`) явно перевіряють, що стара дірка
> не повернеться. Разом змінено 25 файлів; див. §9.

### 5.4. `X-Admin-Secret` збігається з паролем для входу

`api-dev/src/modules/security/admin-auth.ts` авторизує за заголовком `X-Admin-Secret`,
який порівнюється з `env.ADMIN_SECRET` — а це **той самий секрет**, що й пароль у
`handleLogin`. Наслідки: зміна пароля ламає всіх API-клієнтів; порівняння не
constant-time.

**Дія:** або окремий секрет для машинного доступу, або перевести адмін-API на cookie-сесію
(вона вже є і вже перевіряється).

> **ЗАКРИТО 11.09.2026.** Рішення: **жодних секретів у заголовках** — усе через
> наявну cookie-сесію. Аудит під час роботи виявив, що проблема ширша, ніж один
> заголовок: було **три** легасі-поверхні, кожна з яких дублювала вже наявний
> захищений маршрут і авторизувалася секретом у заголовку.
>
> | Поверхня | Було | Чому прибрано |
> |---|---|---|
> | `/db-proxy` | `X-Admin-Secret` == пароль входу | повний CRUD БД (сценарії, `SELECT * FROM users`, `settings`) на публічному URL api-dev; ніщо його не викликало; дублює `/api/admin/*` |
> | `/setup-webhook`, `/webhook-info` | `X-Bot-Token` == `BOT_TOKEN` | витік `BOT_TOKEN` дозволяв **переспрямувати вебхук на чужий сервер** і збирати `initData` всіх користувачів; дублює `/api/bot/*` за cookie |
> | `/auth/check` (старий) | `X-Admin-Secret` | мертвий код (не в роутері) + зливав існування користувача через `?user_id=` — пряме порушення §5.3/AGENTS §7 |
>
> Видалено: `modules/security/admin-auth.ts`, `controllers/auth-check.controller.ts`,
> `controllers/db-proxy.controller.ts`, `services/db-proxy.service.ts`,
> `controllers/webhook.controller.ts` (−~500 рядків, з них ~250 — мертвий
> `DbProxyService`). Заодно прибрано невикористаний `ADMIN_SECRET` з `Env` у `bot-dev`.
>
> **Тепер адмін-авторизація — це один блок коду** в `api-dev/src/router.ts` (адмін-гейт),
> плюс незалежна перевірка того самого токена в `web-admin-dev/src/worker.ts`.
> Два рівні однієї перевірки — навмисно: api-dev має власний публічний URL,
> тому не покладається на те, що перед ним стояв проксі.
>
> **Межу винесено в одну константу й покрито тестами.** Список префіксів —
> `ADMIN_PATH_PREFIXES` (експортований з `router.ts`), а `router.test.ts`
> перевіряє кожен із них. Це важливо, бо найтонший спосіб зламати гейт —
> не змінити його, а додати новий ендпоїнт поруч і забути про префікс.
> Тепер така помилка падає в CI, а не в прод.
>
> **Перевірено на живому проді** одразу після деплою: `/health` — 200,
> `POST /db-proxy` — 404, `GET /setup-webhook` — 404, усі три адмін-префікси
> без cookie — 401, `/api/catalog` та `/api/scenario/home` — 200,
> `/api/my-dates` без initData — 401, `/auth/check` — `{"authenticated":false}`.
>
> **Наслідок для експлуатації:** `ADMIN_SECRET` тепер має рівно одне призначення
> — пароль входу + ключ підпису cookie. Ротація пароля більше нікого не ламає.
> `BOT_TOKEN` лишається секретом бота і не дає доступу до адмінки.

### 5.5. Застарілі числа в документації

`README.md`, `CONTRIBUTING.md` і `AGENTS.md` §8 називали «44 unit-тести», хоча
насправді їх 106 (пізніше 111).

**Дія:** оновити числа і додати план консолідації в індекс документації.

> **ЗАКРИТО 11.09.2026.** `README.md` (бейдж + швидкий старт), `CONTRIBUTING.md`
> (quality gates + таблиця документації) і `AGENTS.md` §8 оновлено (106 → 111 після §5.4).
> Додатково виправлено неточність у `docs/PAGE_ENGINE_ARCHITECTURE.md` §2.4
> (див. §5.2) і застарілий рядок про D1 у `web-admin-dev/README.md`.

### 5.6. `/api/mydate/*` — публічний запис у БД і KV

Ці ендпоїнти не перевіряють ідентичність — і це **правильно**: це калькулятори
(нумерологія, астрологія) з кешем по даті, а не персональні дані. Але разом із
читанням вони виконують `saveAnalysis()` — тобто **хто завгодно може писати в БД і KV**
довільними датами.

Це не витік даних, а питання ресурсів: спамер може наповнити `CONTENT_KV` і таблицю
аналізів. Захист від цього — або rate-limit, або вимога `initData` (він є в Mini App
у будь-якому разі).

**Дія: відкладено до появи реального трафіку.** Перед публічним запуском вирішити:
`initData` (простило) чи Cloudflare Rate Limiting (дешевше за CPU). Зараз будь-який
захист тут дорожчий за ризик.

**Статус:** відкрито, свідомо відкладено.

### 5.7. S-6: CI не гейтив нічого, крім лінта й типів

`.github/workflows/deploy.yml` мав три тихі дірки — саме ті, які найдорожчі
для проєкту, що збирається в прод:

| Було | Наслідок |
|---|---|
| Тести не запускались у CI взагалі (S-6) | 111 тестів писались, але червоний тест не заважав деплою |
| `npm audit` стояв з `continue-on-error: true` | критична CVE у залежностях їхала в прод мовчки |
| `npm install` замість `npm ci` | збірка не була відтворюваною: патч транзитивної залежності змінював прод без жодного коміту |

Плюс дві менші: два пуші поспіль давали **паралельні деплої** (Cloudflare —
last-write-wins, тож старіший коміт міг лягти поверх новішого), і
`GITHUB_TOKEN` мав дефолтні права, хоча деплою він не потрібен.

**Дія:** єдина блокуюча джоба `checks` перед усіма деплоями; `npm ci`;
`npm audit --audit-level=critical` без `continue-on-error`; `npm test`;
тригер `pull_request` (тільки гейти — деплой з PR неможливий через
`github.event_name != 'pull_request'`); `concurrency` із чергою замість
скасування; `permissions: contents: read`.

> **ЗАКРИТО 11.09.2026.** `npm audit --audit-level=critical` проходить
> (у залежностях 6 вразливостей, але всі — moderate/high від ланцюжка
> `wrangler`/`miniflare`). Поріг свідомо лишено на `critical`: на `high`
> деплой блокувався б зараз, а `--force` вимагає мажорних оновлень wrangler.
>
> **Одна річ ще не в CI:** `npm run format:check` падає на **199 файлах**.
> Додавати його як блокуючий зараз — це одночасно 199-файловий рефактор у
> чужому коміті. Порядок правильний: спочатку `npm run format` окремим комітом,
> потім увімкнути гейт. Записано в §3.5.

### 5.8. Моніторинг: про падіння прода дізнавались від користувачів

**Статус:** код зроблено 11.09.2026; зовнішній монітор підключає власник акаунта.

Після закриття безпекового боргу лишалось три сліпі зони:

| Зона | Що це означало на практиці |
|---|---|
| Немає зовнішньої uptime-перевірки | якщо воркер лягав, першими про це дізнавались користувачі, а не ми |
| `/health` перевіряв лише сам воркер | він не торкався D1/KV, тож віддавав `status: "ok"` навіть при мертвій базі — фальшиве заспокоєння |
| Логи | `[observability]` був відсутній у `api-dev`, `web-platform-dev`, `web-admin-dev`; `bot-dev` мав лише `[observability.logs]` без top-level `enabled` |

**Дія 1 — два різні health-ендпоїнти.** Це дві різні відповіді, і змішувати їх не можна:

| Ендпоїнт | Питання, на яке відповідає | Залежності |
|---|---|---|
| `GET /health` | «воркер живий?» | не торкається БД і KV |
| `GET /health/deep` | «дані працюють?» | `SELECT 1` у D1 + `get` у KV; **503**, якщо хоч одна недоступна |

`/health/deep` навмисно **не** повертає текст помилки: ендпоїнт публічний, а
повідомлення D1 бувають детальними (назви таблиць, колонок, SQL). Текст іде в лог
(`apiLog.error`), назовні — лише `checks: { db, kv }`. Обидві відповіді з
`Cache-Control: no-store`, інакше монітор бачив би закешований стан.

**Дія 2 — Workers Logs у всіх 4 воркерах.** Однакова конфігурація:

```toml
[observability]
enabled = true

[observability.logs]
enabled = true
invocation_logs = false
```

`invocation_logs = false` вимикає лише службовий лог кожного запиту (найбільший обсяг,
найменша користь); власні `console.*` та необроблені винятки лишаються — саме тому
безкоштовного ліміту (200 000 подій/добу, зберігання 3 дні) вистачає з запасом.

**Дія 3 — зовнішній монітор (робить власник акаунта).** UptimeRobot, безкоштовний
план: 50 моніторів, перевірка раз на 5 хвилин, без картки. Монітор — HTTP(s) на
`https://api-dev.diskomate.workers.dev/health/deep`, бо саме він падає (503), коли
лежить база, а `/health` на цю роль не годиться.

**Дія 3 — Sentry (зроблено 11.09.2026).** UptimeRobot каже, **що** впало, а Sentry —
**чому**. SDK `@sentry/cloudflare` під'єднано у `api-dev` і `bot-dev`; опції живуть у
спільному `@wwwuabot/shared/observability/sentry`, тож політика даних описана один раз:

| Рішення | Чому саме так |
|---|---|
| `sentryOptions()` вертає `undefined` без `SENTRY_DSN` | деплой без секрету безпечний, а вмикається Sentry одним секретом — без зміни коду |
| `tracesSampleRate: 0` | перформанс зʼїдав би квоту (5 000 помилок/міс), не даючи користі |
| `dataCollection` вимкнено + `beforeSend` ріже заголовки/cookie/тіло/query | у заголовку їде Telegram `initData` (ним можна видати себе за користувача, поки підпис не сплив), у cookie — `admin_session` |
| `withSentry` — єдина обгортка в `index.ts` воркера | необроблені винятки ловляться без try/catch у кожному контролері |
| Явний 500 в `api-dev` | раніше клієнт отримував сирий stack trace Cloudflare; тепер — `{"error":"Internal error"}`, а деталі йдуть у логи й Sentry |
| `bot.catch` звітує сам | помилки, які grammY перехоплює, до `withSentry` не долітають |
| `[version_metadata]` у `wrangler.toml` | у Sentry видно, який саме деплой зламався |

**Без залежностей у shared:** модуль не імпортує SDK у рантаймі — віддає звичайний обʼєкт,
а сумісність із реальним типом перевіряє `satisfies Sentry.CloudflareOptions` на місці
виклику. Так `packages/shared` не тягне серверний SDK у браузерні бандли. Саме цей
`satisfies` одразу й зловив розходження в сигнатурі `beforeSend`.

**Чому не в `web-platform-dev` / `web-admin-dev`:** там помилки живуть у браузері, і DSN
треба вшивати в бандл при збірці (окремий механізм — секрет GitHub Actions + `VITE_`).
Окремий крок, коли з'явиться трафік.

**Поза кодом:** власник акаунта створює проєкт у Sentry і додає секрет `SENTRY_DSN` у
Cloudflare для `api-dev` і `bot-dev` — інструкція в `docs/MONITORING.md`.

**Перевірка:** `health.controller.test.ts` (7 тестів) фіксує найдорожче — 503 при
мертвій D1 і при мертвому KV, відсутність витоку тексту помилки, рівно один запит
до БД, `no-store`.

---

## 6. Порядок виконання

Етапи залежні, але всередині незалежні.

> **ВИПРАВЛЕНО 11.09.2026.** Було 11 помилок у 6 файлах. `npm run typecheck` тепер
> чистий на всіх 6 воркспейсах — CI більше не блокує деплой. Деталі — §9.

```
§5.3  (єдина ідентичність)       ✅ ЗРОБЛЕНО 11.09 — це безпека, не код
   │
§5.8  (моніторинг: health/логи)  ✅ ЗРОБЛЕНО 11.09 — UptimeRobot ← крок власника
   │
§3.1  (HMAC у shared)            ✅ ЗРОБЛЕНО 11.09 — обидва воркери стали тоншими
   │
   ├── §3.2  (apiFetch-фабрика)            ↩ відхилено, див. §3.2
   ├── §3.3  (useMyDates)                  ✅ ЗРОБЛЕНО 11.09 — −51 рядок, 1 баг
   ├── §3.4  (контролери сценаріїв)        ✅ ЗРОБЛЕНО 11.09
   ├── §3.5  (дрібне)                      ← НАСТУПНЕ
   │
§4.2  (index.css адмінки)        ← найбільший механічний виграш, робити окремо
   │
§4    (PageRenderer на сторінки)
```

**Безпековий борг закрито повністю** (§5.1–§5.5, §5.7). Усі адмін-дії тепер
проходять через одну cookie-сесію; секретів у заголовках не лишилось; CI блокує
деплой на будь-якому червоному гейті. Моніторинг (§5.8) — код готовий, лишився
зовнішній UptimeRobot. Відкрито: §5.6 (відкладено свідомо).

**Правило:** один пункт = один коміт = окремий деплой. Кожен пункт можна відкотити
поодинці, бо всі вони зворотносумісні.

---

## 7. Чого цей план НЕ дасть (чесно)

| | |
|---|---|
| **Оцінка виграшу** | ≈ −3 300..4 300 рядків = **8..10% від 20 044** рядків застосунків |
| **Час** | Етап 1: ~1 день. Етап 2: ~2 дні. Етап 3: залежить від перевірки API |
| **Що НЕ зміниться** | Модель деплою, роутинг, бізнес-логіка, кількість воркерів |

**Головний ефект — не рядки.** Він у тому, що зникає потреба правити одне й те саме у двох
застосунках, і зникає клас багів «auth розсинхронився». Рядки — це побічний продукт.

Оцінка «−70..75%» з `PAGE_ENGINE_ARCHITECTURE.md` §6 стосується **окремих файлів**
(572 → 206 — це правда), а не репозиторію. Для репозиторію реалістична стеля — ~10%,
і досягається вона саме так, як вище. Решту 90% можна скоротити **тільки скороченням
обсягу продукту**, а не рефакторингом.

---

## 8. Рішення, які потрібні від тебе

1. **Правило межі з §2** — приймаємо як є, чи коригуємо? _Фактично вже застосовано
   в §5.3/§3.1: логіка перевірки — у shared, рішення при провалі — у кожній оболонці._
2. **Порядок** — ~~починаємо з безпеки (§5.3) чи з механічної економії (§4.2)?~~
   **Вирішено: спочатку безпека.** §5.3, §5.2, §5.1 закрито 11.09.2026.
3. **Дві таблиці сценаріїв** (§3.4) — лишаємо як є чи плануємо міграцію на одну з `scope`?
   **Відкрито.** Дублювання вже прибрано на рівні коду; міграція даних — окреме рішення.
4. ~~**Невикористаний D1-біндинг** у адмінці (§5.2)~~ — **закрито 11.09.2026** (прибрано).
5. ~~**`X-Admin-Secret`** (§5.4) — окремий машинний секрет, чи перейти на cookie?~~
   **Вирішено 11.09.2026: cookie-сесія, всі секретні заголовки видалено.**

---

## 9. Журнал виконаного

### 11.09.2026 — безпека ідентичності (§5.3) + гігієна

| Що | Файли |
|---|---|
| Єдина ідентичність | `api-dev/src/shared/identity.ts` (новий), `+ identity.test.ts` |
| Спільна перевірка `initData` | `packages/shared/src/security/telegram.ts` (новий, +тести) |
| Видалено дубль | `api-dev/src/shared/telegram-auth.ts` (видалено, −62) |
| Контролери переведено на `resolveUserId` | `sites`, `site-pages`, `templates`, `users`, `my-dates` |
| Клієнт надсилає `initData` | `web-platform-dev/src/app/AuthGate.tsx`, `shared/api/client.ts`, `mydate.api.ts`, сторінки site-builder |
| Блок `my-dates-table` | `packages/ui/src/blocks/my-dates-table/api.ts` |

### 11.09.2026 — HMAC-сесія в shared (§3.1) + фабрика сценаріїв (§3.4)

| Що | Файли |
|---|---|
| Спільна сесія | `packages/shared/src/security/session.ts` (новий, +`session.test.ts`) |
| Тонкий `worker.ts` адмінки | `web-admin-dev/src/worker.ts` (−~100) |
| Тонкий `auth.controller.ts` | `api-dev/src/controllers/auth.controller.ts` (−~90) |
| Фабрика сценаріїв | `api-dev/src/controllers/scenarios.controller.factory.ts` (новий) + обидва контролери (~30 рядків кожен, −~280) |
| Оголошена залежність | `web-platform-dev/package.json` (`@wwwuabot/ui`) |

**Перевірка після всього:** `npm test` — 106/106 ✅ · `npm run typecheck` — чисто на
всіх 6 воркспейсах ✅ · `npm run lint` — 0 errors (6 попередніх warnings у `web-admin`) ✅

_Числа тестів нижче зросли до 111 після §5.4 — див. наступний запис._

### 11.09.2026 — єдина точка адмін-авторизації (§5.4)

Аудит усіх маршрутів `api-dev` перед правкою показав, де саме проходить межа
«публічне / `initData` / адмін». Результат:

| Група | Маршрути | Авторизація |
|---|---|---|
| Публічні | `/health`, `/api/mydate/*`, `/api/scenario/:slug`, `/api/catalog*`, `GET /api/templates` | немає (задумом) |
| Користувач | `/api/sites/*`, `/api/sites/:slug/pages/*`, `/api/templates` (write), `/api/my-dates`, `/api/user/profile` | підписаний `initData`, перевірка власника |
| Адмін | `/api/admin/*`, `/api/portal/*`, `/api/bot/*` | cookie `admin_session` (HMAC) |

| Що | Файли |
|---|---|
| Видалено `X-Admin-Secret` | `api-dev/src/modules/security/admin-auth.ts` (+ порожня тека `modules/`) |
| Видалено мертвий ендпоїнт | `api-dev/src/controllers/auth-check.controller.ts` |
| Видалено `/db-proxy` | `controllers/db-proxy.controller.ts`, `services/db-proxy.service.ts` |
| Видалено легасі вебхук-ендпоїнти | `controllers/webhook.controller.ts` |
| Роутер | `api-dev/src/router.ts` — один явний адмін-гейт, коментарії до нього |
| Типи | `api-dev/src/shared/types.ts` (опис `ADMIN_SECRET`), `bot-dev/src/shared/types/env.ts` (прибрано невикористаний `ADMIN_SECRET`) |
| **Знайдено під час аудиту** | `GET /api/templates/:id` не перевіряв власника взагалі — приватна конфігурація читалась за UUID (IDOR) |
| Фікс IDOR | `api-dev/src/controllers/templates.controller.ts` — `canReadTemplate()` + тест (`templates.controller.test.ts`, 5 кейсів) |
| Межа гейта в одній константі | `ADMIN_PATH_PREFIXES` в `api-dev/src/router.ts` |
| Регресійні тести гейта | `api-dev/src/router.test.ts` (15 тестів): підроблена/протермінована/сміттєва cookie, відсутній `ADMIN_SECRET`, секрет у заголовку, 404 на видалених маршрутах, публічні vs користувацькі шляхи |
| Доки | `api-dev/README.md`, `bot-dev/src/api/router.ts` |

**Перевірка:** `npm test` — 126/126 ✅ · `npm run typecheck` — чисто на всіх 6
воркспейсах ✅ · `npm run lint` — 0 errors ✅
**Прод:** задеплоєно `api-dev` і `bot-dev` (run 34584327030 ✅); поведінка
перевірена запитами до живого воркера (див. вставку вище).

> **Що варто знати про `GET /api/templates/:id`:** цей маршрут **не викликає жоден
> клієнт** — ні `web-platform`, ні `web-admin`. Я не видаляв його, бо він входить
> у заявлений REST-CRUD шаблонів і може знадобитись у редакторі. Але якщо він не
> знадобиться — це кандидат на видалення разом з рештою легасі (§3.5).
>
> **`/api/mydate/*` свідомо лишається публічним** — це калькулятори з кешем по
> даті, не персональні дані. Але це неавторизований *запис* у D1/KV: хто завгодно
> може наповнити кеш довільними датами. Не діра в даних, а питання витрат і спаму.
> Закрити після запуску, коли з'явиться реальний трафік (кандидат у §5.6).

### 11.09.2026 — CI блокує деплой (S-6, §5.7)

| Що | Файл |
|---|---|
| Єдина блокуюча джоба `checks` | `.github/workflows/deploy.yml` — `npm ci` → `npm audit --audit-level=critical` → `lint` → `typecheck` → `test` |
| Тести тепер гейтять (S-6 закрито) | там само |
| Відтворювані збірки | `npm install` → `npm ci` у всіх джобах + `cache: npm` |
| Гейти на pull request | `pull_request` тригер; деплой-джоби під `github.event_name != 'pull_request'` |
| Черга деплоїв | `concurrency: deploy-${{ github.ref }}`, `cancel-in-progress: false` |
| Мінімум прав | `permissions: contents: read` |
| Гілка `audit` злита в `checks` | окрема джоба видалена (менше інсталів, один лог для дебагу) |
| Доки | `README.md`, `CONTRIBUTING.md`, `AGENTS.md` §6/§8 |

**Перевірка:** YAML валідний (розібраний парсером, не «на око») ✅ ·
`npm audit --audit-level=critical` exit 0 ✅ · `npm test` — 126/126 ✅ ·
`typecheck` — чисто ✅ · `lint` — 0 errors ✅
**CI на GitHub:** run 34584670467 ✅ — джоба `checks` виконала всі п'ять гейтів
(`npm ci` → аудит → лінт → типи → тести), деплой-джоби коректно пропущені
(у пуші не було коду воркерів).

### 11.09.2026 — моніторинг: `/health/deep` + Workers Logs (§5.8)

| Що | Файли |
|---|---|
| Два health-ендпоїнти | `api-dev/src/controllers/health.controller.ts` — `/health` (liveness) і `/health/deep` (503 при мертвих D1/KV) |
| Роутинг | `api-dev/src/router.ts` — `/health/` і `/health/deep` |
| Workers Logs у всіх 4 воркерах | `api-dev/wrangler.toml`, `bot-dev/wrangler.toml`, `web-platform-dev/wrangler.toml`, `web-admin-dev/wrangler.toml` |
| Тести | `api-dev/src/controllers/health.controller.test.ts` (7): 503, відсутність витоку тексту помилки, один запит до БД |
| Лог | `health.controller.ts` → `apiLog.error` замість сирого `console.error` |

**Перевірка:** `npm test` — 133/133 ✅ · `npm run typecheck` — чисто на всіх 6
воркспейсах ✅ · `npm run lint` — 0 errors (6 warnings у `web-admin`) ✅
**Далі (поза комітом):** підключити UptimeRobot на `/health/deep`, потім Sentry.
_На момент цього кроку тестів було 133; наступний коміт (§3.3) додав 8 → 141._

### 11.09.2026 — один `useMyDates` для сторінки і блока (§3.3)

| Що | Файли |
|---|---|
| Канонічний хук | `packages/ui/src/blocks/my-dates-table/useMyDates.ts` + `useDateFilters.ts`, `useDateSelection.ts`, `useDateModal.ts`, `useMyDates.types.ts` |
| Чисті функції | `filter-sort.ts` — `collectTags`, `filterDates`, `sortDates` |
| Видалено дубль | `web-platform-dev/src/pages/mydate/useMyDates.ts` (253 рядки); `MyDatesPage` імпортує хук із `@wwwuabot/ui/blocks/my-dates-table` |
| Експорт пакета | `packages/ui/package.json` → `"./blocks/my-dates-table"` |
| Тести | `filter-sort.test.ts` (8): фільтри, теги, сортування, незмінність вхідного масиву |
| Виправлено | сортування рядків — `localeCompare("uk")`: «і» більше не після «я» |
| Прибрано | 4 невикористані опції хука (`showSearch`, `showTypeFilter`, `showBulkActions`, `showCreateButton`) і власний `useEffect` сторінки з `eslint-disable` |

**Перевірка:** `npm test` — 141/141 ✅ · `npm run typecheck` — чисто на всіх 6
воркспейсах ✅ · `npm run lint` — 0 errors (6 warnings у `web-admin`) ✅
**Візуально не перевірено:** сторінка «Мої дати» живе в TWA і вимагає Telegram
`initData` — перевірити в мініапці після деплою.
**Далі:** §3.5 (дрібне + розбити `MyDatesPage.tsx`, 381 рядок).

**Додано 11.09.2026:** обробник 500 у `api-dev` і Sentry (§5.8, дія 3), тестів — 149.

### 11.09.2026 — Sentry: стек-трейси помилок (§5.8, дія 3)

| Що | Файли |
|---|---|
| Спільні опції + політика даних | `packages/shared/src/observability/sentry.ts`, `sentry.test.ts` (6) |
| Обгортка воркерів | `api-dev/src/index.ts`, `bot-dev/src/index.ts` — `Sentry.withSentry` (у бота — і `fetch`, і споживач черги) |
| Явний 500-обробник | `api-dev/src/index.ts` — `apiLog.error` + `Sentry.captureException` + JSON без деталей |
| Помилки grammY | `bot-dev/src/core/bot.ts` — `Sentry.captureException` у `bot.catch` |
| Конфіг Cloudflare | `api-dev/wrangler.toml`, `bot-dev/wrangler.toml` — `compatibility_flags = ["nodejs_compat"]` + `[version_metadata]` |
| Типи оточення | `SENTRY_DSN` у `api-dev/src/shared/types.ts` і `bot-dev/src/shared/types/env.ts` |
| Тести обробника 500 | `api-dev/src/index.test.ts` (2): JSON без деталей; звичайний маршрут не зачеплений |

**Перевірка:** `npm test` — 149/149 ✅ · `npm run typecheck` — чисто на всіх 6 воркспейсах ✅ ·
`npm run lint` — 0 errors ✅ · `npx wrangler deploy --dry-run` для обох воркерів ✅
(`api-dev` 586 KiB / 122 KiB gzip, `bot-dev` 678 / 145).

**Поза кодом:** власник акаунта додає секрет `SENTRY_DSN` у Cloudflare — `docs/MONITORING.md`.

**Знайдено дорогою — обидва закрито наступним кроком (див. нижче):**

1. `bot-dev/wrangler.toml` мав `ENVIRONMENT = "dev"`, і саме з ним бот ішов на прод. Впливає
   на текст помилки для користувача і на `environment` у Sentry.
2. `/api/scenario/%` (некоректний `%`) зривав `decodeURIComponent` у `router.ts`. Раніше це
   був сирий 500 від Cloudflare, потім — 500 JSON і подія в Sentry. Правильно повертати
   **400**: кривий ввід не мусить виглядати як збій сервера.

### 11.09.2026 — «поганий ввід ≠ збій сервера» і прод-середовище бота

Причина кроку — та сама, що й у Sentry: **помилка має означати помилку.** Поки 500
з'являвся від сміттєвого запиту, Sentry рано чи пізно почав би панікувати на порожньому
місці, а безкоштовна квота (5 000 подій/міс) витрачалася б на сканери.

| Що | Файли |
|---|---|
| Прод-середовище бота: `"prod"` → `"production"` (одне слово для одного стану) | `bot-dev/src/shared/config/texts.ts`, `texts.test.ts` (2) |
| Безпечне декодування сегмента шляху — `null` замість `URIError` | `api-dev/src/shared/url.ts`, `url.test.ts` (7) |
| **400** замість 500 на битому кодуванні | `api-dev/src/router.ts` — `/api/scenario/`, `/api/mydate/analysis/` |
| Публічний ендпоїнт більше не віддає текст винятку з D1 | `api-dev/src/controllers/scenarios.controller.ts` |
| Тести | `api-dev/src/router-input.test.ts` (5), `api-dev/src/index.test.ts` (1) |

**Перевірка:** `npm test` — 162/162 ✅ · `npm run typecheck` — чисто ✅ · `npm run lint` — 0 errors ✅

**Чому це не косметика:** `/api/scenario/:slug` — **публічний** ендпоїнт (без авторизації),
а його обробник доти віддавав клієнту `e.message` з D1. Тобто назви таблиць і значення
з бази їхали анонімному запитувачу. Тепер назовні — `{ ok: false, error: "Internal error" }`,
а деталі — в Workers Logs і Sentry. Тест обробника 500 підміняє роутер (`vi.mock`) і
перевіряє, що у відповіді немає **жодного** фрагмента внутрішньої помилки.

### Що лишилось (за спаданням користі)

1. ~~**ЗАКРИТО 11.09.2026: §3.3 — один `useMyDates`** (253 + 245 рядків → один хук) — ~−240 рядків.~~
2. **§4.2 — `web-admin-dev/src/index.css` (3 148 рядків)** — найбільша механічна
   економія, потребує покрокової візуальної перевірки, тому окремо.
3. **§3.5 — дрібне** (`app.store` re-export, alias `@/*` в адмінці, `profile.css`) +
   прибрати **6** ESLint-warnings (усі —
   `set-state-in-effect` у React-сторінках. Errors — 0, тому не блокує, але шуміє.
   Тут же — `npm run format` (199 файлів) і одразу після нього гейт `format:check`
   у CI, який зараз свідомо вимкнено.
4. ~~**§5.4 — `X-Admin-Secret`**~~ — **закрито 11.09.2026**, див. §5.4 і журнал нижче.
5. ~~**§5.7 — CI не гейтив тести й аудит**~~ — **закрито 11.09.2026**, див. §5.7.
6. ~~**Sentry** (§5.8)~~ — **код зроблено 11.09.2026** (`api-dev`, `bot-dev`); лишається
   секрет `SENTRY_DSN` від власника акаунта. Стек-трейси для браузерних застосункiв —
   окремий крок (потрібен `VITE_`-DSN на етапі збірки).
