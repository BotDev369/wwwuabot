# Документація проєкту: покажчик

**Один документ = одна тема = один власник факту.** Якщо число чи правило потрібне в двох
місцях, його тримає один документ, а інші на нього посилаються.

## Де що лежить

| Документ | Відповідає за |
|---|---|
| [`../AGENTS.md`](../AGENTS.md) | **правила роботи**: архітектура, межі, доменні терміни, заборони, конвенції, гейти, відкрита робота. Єдиний документ, який читають повністю |
| [`RECIPES.md`](./RECIPES.md) | **як робити типові задачі**: новий ендпоїнт, колонка D1, кирпичик, екран платформи, екран бота, спільний код, тест |
| [`DESIGN_SYSTEM.md`](./DESIGN_SYSTEM.md) | токени й **наскрізні** правила вигляду (нумеровані — на них посилається код), бренд, теми. Склад класів — у CSS і `check:css` (перелік у документі дрейфує) |
| [`SURFACES.md`](./SURFACES.md) | поверхні: аркуш (`.wb-sheet`), композер, списки вибору — правила **13** і **17** |
| [`COLLECTIONS.md`](./COLLECTIONS.md) | колекції: нотатки й контакти, спільна смуга керування — правила **18** і **20** |
| [`SPACE.md`](./SPACE.md) | відкрите й приватне: публічний профіль, вибір полів, що бачать інші, Простір |
| [`THEMES.md`](./THEMES.md) | тема: схема (три кольори + шрифт), бібліотека власних схем, публічність, сторінки теми й друга смуга футера |
| [`API.md`](./API.md) | усі шляхи `api-dev` із групами доступу. **Генерований** з роутера — правити не можна, оновлює `npm run doc:api` |
| [`DATA_MODEL.md`](./DATA_MODEL.md) | таблиці D1: власник, хто створює, хто читає; як виконати SQL; гейт `check:db` |
| [`CONTENT_MODEL.md`](./CONTENT_MODEL.md) | контент сторінок: одне сховище (`scenarios`), адреса — одна сутність `slug` |
| [`PAGES.md`](./PAGES.md) | сторінки з шаблону: два шаблони, власник і публічність, адреса з назви, створення з перегляду шаблону |
| [`SHOPS.md`](./SHOPS.md) | магазин: вітрина як рядок `scenarios`, товари й каталог, види товару, фото в R2, замовлення, статуси й оповіщення продавця |
| [`QUALITY_GATE.md`](./QUALITY_GATE.md) | планка в CI: чотири правила, бюджет розміру, леджер боргу, чого ще немає |
| [`MONITORING.md`](./MONITORING.md) | health, логи, Sentry, UptimeRobot; зрізи показників проєкту (код, GitHub) на `/monitoring`; що робити при падінні |
| [`../bot-dev/README.md`](../bot-dev/README.md) | бот як система: конвеєр апдейта, `ctx.screen`, рендер, черга нотифікацій, контакти |
| [`HOME_PAGE_DATA.json`](./HOME_PAGE_DATA.json) | дані головної сторінки (вміст `page_data`) для міграції `2026-09-14-scenarios-03-home-page.sql` |

## Куди писати нове

| Що з'явилось | Куди |
|---|---|
| Нове правило, заборона, конвенція | `AGENTS.md` (відповідний §) |
| Нова тема, яка заслуговує документа | новий маленький `.md` у `docs/` + рядок у таблиці вище |
| Новий рецепт | [`RECIPES.md`](./RECIPES.md) |
| Новий гейт або поріг | [`QUALITY_GATE.md`](./QUALITY_GATE.md); якщо гейт про одну тему — документ цієї теми (напр. `check:db` — у [`DATA_MODEL.md`](./DATA_MODEL.md)) |
| Новий токен або наскрізне правило вигляду | [`DESIGN_SYSTEM.md`](./DESIGN_SYSTEM.md) (номер правила не змінювати — на нього посилається код) |
| Правило про конкретну поверхню або колекцію | документ її теми — [`SURFACES.md`](./SURFACES.md) / [`COLLECTIONS.md`](./COLLECTIONS.md); номер правила лишається в [`DESIGN_SYSTEM.md`](./DESIGN_SYSTEM.md) |
| Новий клас | спочатку правило в `packages/shared/src/styles/`, тоді вжиток; перелік — `AGENTS.md` §3 |
| Нова іконка | `packages/shared/src/components/icons.tsx` (`IconName` — єдине джерело) |
| Новий ендпоїнт | нікуди вручну: `npm run doc:api` перегенеровує [`API.md`](./API.md) |
| Нова таблиця або колонка D1 | реєстр `packages/shared/src/database/tables.ts` + рядок у [`DATA_MODEL.md`](./DATA_MODEL.md) |
| Правило адреси, діплінка чи параметрів URL | [`CONTENT_MODEL.md`](./CONTENT_MODEL.md) + код `packages/shared/src/content/resolve.ts` |
| Виміряне число | **нікуди**: його друкують самі гейти (`npm run check:css` / `check:docs` / `check:quality` / `npm test`) |

## Бюджет розміру — два різні, і в кожного є права

Код і документація — різні жанри, тому ліміт у кожного свій. Межі тримають гейти, а не пам'ять
автора: код — `npm run check:quality`, документи — `npm run check:docs`.

| Група | М'яка межа (попередження) | Жорстка межа (помилка) | Де право на більше |
|---|---|---|---|
| Код | 200 рядків | 400 рядків | `scripts/quality-baseline.mjs` — леджер боргу, тільки зменшується |
| Документ | 200 рядків **або** 24 KB | 400 рядків | `BUDGET_RIGHTS` у `scripts/check-docs.mjs` — кожне право з причиною |

**Право на більше — це не виняток для улюбленця, а записана причина.** Файл, який справді не
ділиться, отримує зсунуту м'яку межу разом із поясненням, чому саме він: без причини право через
місяць «оптимізують» назад. Сьогодні таке право **одне**: [`../AGENTS.md`](../AGENTS.md) — єдина
інструкція для агентів, яку читають повністю одним заходом. Правило, яке перестало потребувати права,
право **втрачає**: документ, що вліз у типову межу після поділу на теми, зі списку зникає (так
звільнився `DESIGN_SYSTEM.md`, коли поверхні й колекції поїхали у свої документи).

**Жорстка межа не зсувається нікому:** файл, який її перейшов, не читають — його гортають.

- **Вага рахується разом із рядками.** Полотно з довгих абзаців і полотно з коротких — однакова
  проблема при однаковій кількості рядків, і саме вага (а не рядки) з'їдає контекст.
- **Генеровані документи розмір не нормують** — їхній розмір визначає код, а гейт перевіряє
  **свіжість**: [`API.md`](./API.md), який розійшовся з роутером, — помилка.
- **Файли-дані** (таблиці констант на кшталт `icons.tsx`) у бюджет коду не рахуються: дробити їх
  навпіл — шкодити.
- **Правило росте не текстом, а адресою.** Нове правило — рядок у `AGENTS.md` (з причиною одним
  реченням); тема, довша за три рядки, їде в окремий маленький `.md` за покажчиком.
- **Реєстр руками не пишуть.** Перелік класів, ендпоїнтів, таблиць, іконок дрейфує першим, тому
  його або друкує гейт (`check:css`, `check:db`, `doc:api`), або він живе в одному місці й
  перевіряється на живість (шляхи, посилання, `AGENTS.md §N` — теж `check:docs`).

Причина, чому бюджет взагалі є: **довгий документ неможливо правити акуратно.** Щоб змінити абзац у
полотні, треба відтворити довгий якір байт-у-байт — і будь-яка невидима розбіжність робить правку
неможливою, причому мовчки. Тому стеля стоїть **нижче** за реальну шкоду: вона робить зростання
видимим за місяць до того, як документ стане некерованим, а не в день, коли правити вже нічого.
