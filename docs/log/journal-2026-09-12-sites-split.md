<!-- журнал 12.09: поділ sites.service.ts -->
> **Частина архіву консолідації.** Покажчик — [`docs/CONSOLIDATION_LOG.md`](../CONSOLIDATION_LOG.md).

### 12.09.2026 — поділ `sites.service.ts` (777 → 6 модулів)

**Було:** один файл на 777 рядків — найбільший у репозиторії й найризикованіший домен
(помилка тут = зламана публікація сайту користувача), при цьому з **нульовим** покриттям
тестами. Сім підрозділів в одному файлі: створення таблиць, CRUD сайтів, CRUD сторінок,
шаблони, модерація, каталог.

**Стало — `api-dev/src/services/sites/`:**

| Модуль | Що в ньому | Рядків |
|---|---|---:|
| `schema.ts` | `CREATE TABLE IF NOT EXISTS` для трьох таблиць — спільна залежність решти | 114 |
| `crud.ts` | CRUD сайтів (+ перша сторінка `home` разом зі створенням сайту) | 178 |
| `pages.ts` | CRUD сторінок | 177 |
| `templates.ts` | CRUD шаблонів (системні — тільки для читання) | 157 |
| `moderation.ts` | статусний автомат `draft → pending → published/rejected` + адмін-списки | 145 |
| `catalog.ts` | публічний каталог (`published` **і** `is_public`) | 72 |

**Чому саме такі межі.** Модерація відділена від `crud` не за розміром, а за сенсом: якби
статус мінявся через `updateSite`, сайт можна було б опублікувати повз чергу. Каталог
відділений як єдиний модуль без авторизації. Граф залежностей — дерево без циклів:
`schema ← {crud, pages, templates} ← moderation`, `pages ← catalog`.

**Доказ, що це перенос, а не переписування:** порівняння з версією з `HEAD` —
26 експортів до і **26** після (нічого не загублено, не додано, без дублікатів), і **кожне
нормалізоване тіло функції знайдено в новому коді дослівно**. 5 контролерів переведено на
прямі імпорти з модулів (`crud` / `pages` / `moderation` / `templates` / `catalog`), моноліт
видалено — посилань на `sites.service` у репозиторії не лишилось. Окремо прогнано
`wrangler deploy --dry-run`: усі нові шляхи резолвляться і в бандлері.

**Перевірка:** `npm run check:quality` — 0 помилок, найбільший файл 443 (був 777), леджер
зменшився до 3 ✅ · `npm run check:css` — 0 ✅ · `npm run lint` — 0/0 ✅ ·
`npm run typecheck` — чисто на 6 воркспейсах ✅ · `npm test` — 182/182 ✅ ·
`npx prettier --check .` — чисто ✅

**Що знайдено попутно (не чіпано):** два мертві експорти — `getSiteById` і `getSitePage` —
оголошені, але не вживаються ніде; це було й до поділу. Це рішення про API-поверхню
сервісу, а не технічний борг, тому лишено як є.

**Наступний крок за планом:** тести для цих шести модулів (пункт 1 плану, крок 2 аудиту §6 —
найбільший із тих, що лишились). Модулі тепер мають рівно той розмір, за якого тест на
кожен домен лишається читабельним: статусний автомат модерації, право власника, каталог.
