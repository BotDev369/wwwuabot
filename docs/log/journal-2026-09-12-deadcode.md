<!-- журнал 12.09: мертвий код, аудит якості, документація -->
> **Частина архіву консолідації.** Покажчик розділів — [`docs/CONSOLIDATION_LOG.md`](../CONSOLIDATION_LOG.md).
> Числа тут — на дату свого запису, не «останні».

### 12.09.2026 — мертвий код, аудит якості, документація за новою реальністю

**1. Знайдено й видалено 74 недосяжні файли (5 238 рядків, 15% коду).** Не очима — обхідом
графа імпортів від точок входу (`main.tsx`, `worker.ts`, `router.tsx`, `exports` пакетів),
включно з динамічними `import()` (саме тому живий `features/editor/richtext/RichTextField.tsx`
не зачеплено). Перед видаленням перевірено три речі: жоден роутер не вказує на ці сторінки,
жоден тест їх не імпортує, `adminNav.store` не має пунктів, що ведуть у видалені файли.

| Острів | Файли | Рядки | Хто замінив |
|---|---:|---:|---|
| Легасі-`mydate` платформи (`pages/mydate/*`, `shared/api/mydate.api.ts`, `stores/app.store.ts`) | 13 | 1 909 | спільні блоки `packages/ui` + Page Builder |
| Легасі-редактор блоків адмінки (`features/editor/blocks/**`, `store.ts`, `pages/editor/**`) | 48 | 2 270 | `features/page-builder/` |
| Сторінки-сироти (`DynamicPage`, `DynamicAdminPage`, `HomePage`, `ProfilePage`) | 4 | 545 | `ScenarioPage` (усе зі сценаріїв) |
| Легасі-модерація, шими, мертві хелпери бота | 9 | 514 | спільний `SaveActionButtons`, `adminNav.store` |

Після видалення недосяжних модулів у `src` — **нуль**.

**Важливе уточнення, щоб не було завищених очікувань:** вага бандла **не змінилась**
(адмінка 545 KB, платформа 411 KB). Мертвий код і так tree-shake-ився — борг був
у репозиторії (читання, пошук, хибні орієнтири при пошуку утиліт), а не в рантаймі.

**2. Аудит якості за професійною рубрикою.** Створено `docs/CODE_QUALITY_AUDIT.md`:
10 критеріїв (ISO/IEC 25010, CISQ, SIG/TÜViT, WCAG 2.2, плюс локальні правила `AGENTS.md`),
у кожного — вага й перевірки з адресою в коді.

| Критерій | Вага | Оцінка |
|---|---:|---:|
| Кристалевість і структура | 12 | 3,8 |
| Типобезпека | 8 | 7,5 |
| Тести | 15 | 6,0 |
| Мертвий код і гігієна | 8 | 6,3 |
| Межі шарів і дублювання | 8 | 6,0 |
| Мобільність і доступність | 15 | 5,7 |
| Дизайн-система | 10 | 5,0 |
| Безпека | 12 | 5,8 |
| Продуктивність і вага бандла | 4 | 2,5 |
| Документація і процес | 8 | 8,8 |
| **Разом** | **100** | **58%** (578,5 / 1000) |

Головний висновок: проєкт тримає **L3 («визначений»)** — правила писані, гейти блокують
деплой, — але **L4 («вимірюваний») відсутній**: у CI немає жодного числового порогу.
Тому пунктом 1 плану стоїть саме планка в CI, а не черговий рефакторинг.

**3. Документи звірено з новою реальністю:** `AGENTS.md` → 2.4 (масштаб переміряно,
прибрано `src/stores/app.store.ts`, якого вже немає), `CONSOLIDATION_PLAN.md` §0/§2/§3/§6,
`PAGE_ENGINE_ARCHITECTURE.md` §1/§3/§6, `SITES_SPEC.md` §8.4/§8.5 і фази 5–6.

**Перевірка:** `npm run typecheck` — чисто на 6 воркспейсах ✅ · `npm run lint` — 0 errors,
0 warnings ✅ · `npx prettier --check .` ✅ · `npm test` — **182/182** ✅ · збірки
`web-platform-dev` і `web-admin-dev` ✅ · CI — зелений.

**Далі:** пункти 0–8 із `docs/CODE_QUALITY_AUDIT.md` §6 (планка в CI → тести на `sites` →
блоки на `.wb-block-*` → розбити 4 файли >400 → мобільний борг → мертвий CSS →
`SELECT *` / `npm audit` → розбити бандли).

**Закрито:** §5.3 (ідентичність),
§3.1 (HMAC сесії), §3.4 (контролери сценаріїв), §5.1 (`@wwwuabot/ui`), §5.5 (застарілі
числа в доках), §5.2 (D1-бінд­инг адмінки + `AGENTS.md`), §4.2 (мертвий CSS адмінки —
**з іншим висновком, ніж у плані**), §4.3 крок 1–2 (мобільний + один дизайн), §4.4 (діалоги).
Свідомо відхилено: §3.2. Свідомо відкладено: §5.6, §8.3.
Моніторинг (§5.8): код готовий — `/health/deep` + Workers Logs; UptimeRobot підключає власник.

**Остання перевірка (12.09.2026):** `npm test` — 182/182 ✅ · `npm run typecheck` — чисто на
всіх 6 воркспейсах ✅ · `npm run lint` — 0 errors, 0 warnings ✅ · `npx prettier --check .` ✅ ·
CI run `34689686979` — усі 6 джобів ✅ (checks + 4 деплої).
Живе: `/api/scenario/%` віддає **400** (було 500), адмін-префікси — 401, публічні — 200.
Секрет `SENTRY_DSN` додано в `api-dev` і `bot-dev` власником акаунта. Деталі — §9.

---

