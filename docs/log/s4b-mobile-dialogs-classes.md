<!-- §4.3–§4.5: мобільний, діалоги, клас без стилю -->
> **Частина архіву консолідації.** Покажчик розділів — [`docs/CONSOLIDATION_LOG.md`](../CONSOLIDATION_LOG.md).
> Числа тут — на дату свого запису, не «останні».

### 4.3. Мобільний пріоритет і єдиний дизайн оболонок (нове, 12.09.2026)

**Пріоритет власника:** мобільний — №1 (єдина точка взаємодії користувачів — Telegram
Mini App, тобто телефони), а `web-platform-dev` і `web-admin-dev` мусять мати **один
дизайн**: різниця лише в логіці. Це вже не побажання, а правило — `AGENTS.md` §3.

**Зроблено (крок 1):**

| Що | Де |
|---|---|
| `viewport-fit=cover` — без нього `env(safe-area-inset-*)` дорівнює 0 | обидва `index.html` |
| `--safe-top/--safe-bottom` враховують і `--tg-safe-area-inset-*` від Telegram | `tokens.css`, `apple.css`, `android.css` |
| `safe-area` під контентом TWA (main, footer, `page-zone--footer`) | `web-platform-dev/src/index.css` |
| `100dvh` замість голого `100vh` (з фолбеком) | адмінка (splash/login/app-root), платформа (page-layout, drawer, table-wrap), `components.css` |
| Мобільна оболонка адмінки: бічна панель стає виїзним drawer'ом — hamburger, скрим, Escape, закриття по пункту меню | `layout/AppShell.tsx`, `layout/Sidebar/*`, нові `useIsMobile.ts`, `useMobileNav.ts`, `useCollapsedNav.ts` |
| Спільні токени drawer'а замість магічних чисел у кожній оболонці | `--drawer-w`, `--scrim` |
| На мобільному «згорнуте меню» не застосовується (drawer із самих іконок нечитабельний) | `useCollapsedNav.ts` |
| Тач-афорданси: `@media (hover: none)` показує `.block-actions` (кнопки ↑ ↓ ✕) | `web-admin-dev/src/index.css` |
| Голова адмінки: `lang="uk"` і нормальний `<title>` | `web-admin-dev/index.html` |
| `100dvh` у мобільних медіа-модалках (стояло тільки `100vh`) | `web-admin-dev/src/index.css` |
| Мобільний каркас редактора сайту (`/sites/:slug`): панель і полотно одне над одним замість фіксованих 320px бічної панелі | `web-platform-dev/src/index.css`, `SiteEditorPage.tsx` |

**Зроблено (крок 2 — «єдиний дизайн» по-справжньому, 12.09.2026):**

1. ~~**CSS спільного рендерера живе лише в одній оболонці.**~~ **ЗАКРИТО** (`f385900`):
   14 селекторів (`page-layout`, `page-zone*`, `page-hamburger*`, `page-sidebar-*`) переїхали в
   `packages/shared/src/styles/page-layout.css`; у платформі не лишилося жодного (звірено зі
   станом до зміни). Override для рамки телефона (`.phone-frame-body .page-*`) лишився в адмінці.
2. ~~**Drawer — спільний примітив.**~~ **ЗАКРИТО** (`87d4d9a`):
   `packages/shared/src/styles/drawer.css` — контейнер `aside.app-drawer` (+ `--open`), скрим
   `.app-drawer-overlay`, кнопка `.hamburger`. Обидві оболонки використовують ті самі класи,
   різниця лише в складі меню. Селектор `aside.app-drawer` (0,1,1) навмисно перемагає
   `.sidebar--collapsed` (0,1,0) без `!important` — `!important` зламав би прев'ю в рамці
   телефона, яке перекриває ті самі класи. Побічно знайдено й виправлено: базових стилів
   `.hamburger` не існувало зовсім (лише брендові зміни висоти смужок), тож кнопка меню
   в платформі була без жодного видимого знака.
5. ~~**`SiteEditorPage.tsx` — 686 рядків і багато inline-стилів.**~~ **ЗАКРИТО**:
   686 → **48 рядків**. Тепер це композиція, а поруч — `pages/site-editor/` з 11 файлів
   (найбільший 176). Інлайн-стилі замінені класами `.site-editor-*`, картки сторінок стали
   `<button>` (працюють і з клавіатури, і пальцем — тап-таргет 44px).

**Лишається (крок 3):**

3. **Локальні класи замість `.wb-*`.** В адмінці є власні `.usr-*` (таблиця, модалки,
   форми) і `.scn-*`, які дублюють уже наявні спільні `.wb-data-table`, `.wb-modal-*`.
   Це найдорожчий крок до «одного дизайну» — тільки з візуальною перевіркою.
4. **Резиновість:** перевірити решту фіксованих `min-width` (напр. `.usr-table` 560px у
   мобільному медіа — чи має скрол-контейнер; він є, `.usr-table-wrap { overflow: auto }`)
   і таблиці з модалками на 360px.
6. **Спільні блоки Page Builder стилізовані лише в платформі.**
   `packages/ui/src/blocks/my-dates-table/DateModal.tsx` використовує `.modal-overlay`,
   `.modal`, `.modal-actions`, `.form-group`, `.tags-input`, `.tag-remove` — усі визначені
   **тільки** в `web-platform-dev/src/index.css`. В адмінці ця модалка (блок «Мої дати» у
   прев'ю Page Builder) рендериться без оверлея й позиціонування. **Дія:** перевести на
   `.wb-modal-*` і прибрати мертві `.modal-*` з платформи.
7. **Дві реалізації тієї самої модалки дати:** `packages/ui/src/blocks/my-dates-table/DateModal.tsx`
   і `web-platform-dev/src/pages/mydate/DateModal.tsx` — різні класи, різні пропси.
   Після кроку 6 звести до однієї.

### 4.4. Діалоги: 33 місця, де UI питав користувача способом, який не працює (12.09.2026)

**Знайдено під час кроку 2.** Платформа — це Telegram Mini App, а нативні
`window.alert`, `window.confirm` і `window.prompt` у WebView на iOS не мають нативної
в'юхи: `prompt` повертає `null`, `confirm` — `false`, `alert` не показується взагалі
(документована поведінка WKWebView; Telegram їх не мостить). Наслідки на телефоні були тихі:

| Місце | Що відбувалось на iPhone |
|---|---|
| «Додати сторінку» (`SiteEditorPage`) | `prompt` → `null` → кнопка не робила нічого |
| Видалення дати / сторінки / шаблону / користувача | `confirm` → `false` → видалення не відбувалось |
| Помилки створення й збереження | `alert` не показувався → людина не знала, що зламалось |
| Відхилення сайту в адмінці | `prompt` → `null` → сайт **відхилявся без причини** |

Тобто найпоширеніший сценарій Mini App був непрацездатний саме на iPhone.

**Рішення — один спільний діалог** `@wwwuabot/ui/dialog`: `DialogProvider` біля кореня
застосунку плюс `useDialog()` з `alert` / `confirm` / `prompt`, що повертають проміси.
Малюється тими самими `.wb-modal-*`, тож вигляд однаковий в обох оболонках. Черга запитів
гарантує, що два діалоги поспіль не «з'їдять» один одного; якщо провайдера немає — хук
відкочується на `window.*`, щоб блок із `packages/ui` не падав у застосунку без провайдера.

| Файл | Роль |
|---|---|
| `packages/ui/src/dialog/{types,DialogContext,DialogHost,DialogProvider,index}.ts(x)` | примітив (21 + 30 + 60 + 100 + 116 рядків) |
| `.wb-dialog*` у `packages/shared/src/styles/components.css` | вигляд: 420px завширшки, `height: auto`, кнопки на всю ширину, `min-height: 44px` |
| `web-platform-dev/src/main.tsx`, `web-admin-dev/src/main.tsx` | `DialogProvider` біля кореня |

**Замінено 33 виклики:** платформа — `site-editor/*` (14), `mydate/DateModal`, спільні
блоки `my-dates-table` (2); адмінка — 17 у 7 файлах (`BotSettingsPage`, `ScenariosPage`,
`SitesPage`, `SitesModerationPage`, `TemplatesPage`, `UserBulkBar`, `UsersTable`).

> Був і восьмий файл — `pages/users/UserRowMenu.tsx`. Його видалено 12.09.2026 як мертвий
> дубль: він не імпортувався ніде, а `UsersTable` має власне меню на спільних
> `.wb-modal-menu*` (див. §4.5).

Побічно виправлено логіку: скасування `prompt` при відхиленні сайту більше не означає
«відхилити без причини».

**Перевірка:** `npm run typecheck` — чисто ✅ · `npm run lint` — 0 errors, 0 warnings ✅ ·
`npx prettier --check .` ✅ · `npm test` — 182/182 ✅ · збірки обох застосунків ✅,
`.wb-dialog` є в обох зібраних CSS ✅.

---

### 4.5. «Клас без стилю»: мертві `wb-*`, спільна модалка на приватному CSS (12.09.2026)

**Питання, яке це відкрило:** «чому в коді досі є власні CSS, якщо стилі ми імпортуємо зі
спільного?» Відповідь виміряна, і вона не про дублювання:

| Шар | Рядків CSS | Що в ньому |
|---|---|---|
| `packages/shared/src/styles/` (9 файлів) | 3 474 | токени, теми, два бренди, reset, `.wb-*`-примітиви, `page-layout`, `drawer` |
| `web-platform-dev/src/index.css` | 1 223 | власні сторінки платформи (мʼякі дати, порівняння, картки) |
| `web-admin-dev/src/index.css` + `profile.css` | 2 473 + 84 | каркас адмінки, таблиці, редактор блоків, login |

`@import` спільного стоїть **в обох** `index.css` — це правда. Але спільне — це базовий шар,
а не «всі стилі»: композиції рівня сторінки (sidebar, topbar, login, редактори, таблиці)
ніколи не переїжджали. Реального дублювання між оболонками майже немає (перетин — **один**
клас, `wb-btn`): вони не дублюють, а **розійшлися** (адмінка тримає `.usr-*`/`.scn-*` там,
де платформа має власні класи).

**Що знайдено (усе — пошуком і заміром, не на око):**

- **44 класи `wb-block-*`, які рендерить `packages/ui`, не мали жодного правила CSS** у
  репозиторії (метод: усі класи з `className` у `packages/ui/src` зіставлено з селекторами
  всіх `.css` репозиторію). Блоки стилізовані inline (`style={{ … }}` — 192 обʼєкти,
  30 хардкод-`hex`, 8 `rgba()`), тому `data-brand` і `data-theme` до них **не доходять**.
  Плюс один клас без правила — `.site-nav-label` (текстовий спан усередині стилізованого пункту).
- **10 утиліт існували лише в розмітці:** `.wb-mt-1..4`, `.wb-mb-2..4`, `.wb-font-bold`,
  `.wb-font-semibold`, `.wb-text-primary` — 17 вживань у спільних блоках. Відступи, вага
  й колір просто не зʼявлялись. Родину вже було частково описано (`.wb-flex*`, `.wb-gap-*`,
  `.wb-text-sm`) — її **доповнено**, а не створено заново.
- **Спільна `my-dates-table/DateModal.tsx` трималась на приватному CSS платформи:**
  `.modal-overlay/.modal/.modal-header/.modal-close/.modal-body/.modal-actions` — лише
  `web-platform-dev/src/index.css`; а `.form-group`, `.tags-input`, `.tag-input`,
  `.tag-suggestions` не існували **взагалі ніде**. Переведено на спільний `.wb-modal-*`
  (той самий, що в діалогах §4.4) і нові `.wb-field`, `.wb-tags-input`, `.wb-chip-sm`,
  `.wb-tag-remove`, `.wb-tag-suggestions`; глифи `✕` → `<Icon name="close" />` (правило 9).
- **`.wb-spinner`** викликався в `SiteNewPage`, правил не було → індикатор не крутився.
  Додано разом із `@keyframes wb-spin`.
- **Адмінка:** `.page-topbar` стояв у розмітці трьох сторінок (Sites / Templates /
  Moderation) без жодного правила — шапка не мала ні відступів, ні лінії; `.block-label`
  (4 місця) замінено на спільний `.wb-field`; сторінка модерації мала inline-`calc(100vh -
  56px)` і фіксовані 360px черги → класи `.sites-split*` з `dvh` (із `vh`-фолбеком)
  і складанням у стовпчик на ≤768px, без inline-стилів.
- **`pages/users/UserRowMenu.tsx` — мертвий файл:** не імпортувався ніде, а його класи
  `.usr-menu*` і були тим «CSS без правил». Те саме меню `UsersTable` уже малює на спільних
  `.wb-modal-menu*`. Видалено (54 рядки).

**Перевірка:** `npm run lint` — 0 errors, 0 warnings ✅ · `npm run typecheck` — чисто на
6 воркспейсах ✅ · `npx prettier --check .` ✅ · `npm test` — 182/182 ✅ · збірки
`web-platform-dev` і `web-admin-dev` ✅.

⚠️ **Візуально не перевірено** — превʼю воркерів у цьому середовищі немає. Найперше варто
глянути на телефоні модалку дати (тепер `.wb-modal`: `max-width` 480 → 560, висота до
`90dvh`) і сторінку модерації сайтів.

**Що лишається з цього напрямку:** прибрати тепер мертві `.modal-*`/`.tags-*` з платформи
(там ще власні сторінки), і порціями переносити inline-стилі `wb-block-*` у `components.css`
— це пункт 3 у §6.

---

