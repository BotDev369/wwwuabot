/**
 * Безпечна робота з сегментами шляху.
 *
 * @module api-dev/src/shared/url
 */

/**
 * Декодує сегмент шляху або повертає `null`, якщо кодування некоректне.
 *
 * `decodeURIComponent` кидає `URIError` на будь-якому «обірваному» `%`:
 * `/api/scenario/%`, `/api/scenario/%zz`. Без цієї перевірки виняток
 * вилітав із воркера, і **кривий запит виглядав як збій сервера**:
 * клієнт отримував 500, а Sentry — помилку рівня «щось зламалось».
 *
 * Сміттєвий трафік (сканери, обрізані посилання, битий `encodeURIComponent`
 * у клієнті) не має ні витрачати квоту помилок, ні піднімати тривогу.
 *
 * `null` тут означає саме «поганий ввід» — і виклик має відповісти **400**,
 * а не 500. Різниця принципова: 500 каже «ми зламались», 400 — «ти приніс
 * сміття», і це різні дії під час розбору інциденту.
 */
export function decodePathSegment(raw: string): string | null {
  try {
    return decodeURIComponent(raw);
  } catch {
    return null;
  }
}
