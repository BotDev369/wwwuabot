# PROJECT_PLAN.md — робочий план wwwuabot

> **Розташування:** `public docs/PROJECT_PLAN.md`
> **Версія документа:** 1.0
> **Останнє оновлення:** 27.08.2026 (оновлено Buffy — AI-агент)
> **Поточний загальний бал проєкту:** 5.4 / 9 (детальна методика — `public docs/SCORECARD.md`)

Це головний робочий документ проєкту. Він самодостатній: щоб зрозуміти,
що робити далі й чому, достатньо прочитати цей файл. Два супутні
документи (`AUDIT.md` — детальний код-аудит, `SCORECARD.md` — система
оцінки і методика балів) містять глибші пояснення для окремих задач,
але не обов'язкові для того, щоб почати працювати.

---

## ⚠️ Інструкція для AI-агентів — прочитати перед будь-якою задачею

1. **Перед стартом задачі** знайди в розділі "Задачі за фазами" перший
   пункт зі статусом ⬜ (Заплановано), рухаючись по фазах зверху вниз
   (Фаза 0 → Фаза 6). Не перескакуй фазу, якщо в ній ще є незакриті
   задачі з вищим пріоритетом, окрім випадків, коли власник проєкту
   явно попросив інше.
2. **Онови статус на 🔄 (В роботі)** прямо в цьому файлі, перш ніж
   почати змінювати код — так власник бачить, що відбувається, навіть
   якщо задачу веде інша AI-сесія паралельно.
3. **Дотримуйся "Критерію готовності (DoD)"**, вказаного в задачі. Якщо
   в процесі з'ясувалось, що задача більша/інша, ніж описано — не мовчки
   розширюй її, а допиши коментар під задачею в форматі
   `> Примітка (дата, модель): ...`.
4. **Після виконання:**
   - постав ✅ (Виконано) навпроти задачі в її фазі;
   - додай рядок у таблицю **"Журнал виконаних задач"** внизу файлу
     (дата, ID задачі, короткий підсумок що саме зроблено, посилання
     на коміт/PR якщо є);
   - якщо задача впливає на один із 10 критеріїв `SCORECARD.md` —
     познач це в колонці "Критерій" (вона вже проставлена в задачах
     нижче) і за потреби онови бал у `SCORECARD.md` окремим кроком.
5. **Якщо під час роботи знайшов нову проблему**, не описану тут —
   не виправляй її "мовчки" в межах поточної задачі (це знову створить
   несистемні зміни, з якими й боремось). Додай запис у розділ
   **"Нові знахідки"** внизу файлу з коротким описом і пропонованим
   пріоритетом. Власник проєкту вирішує, чи додавати її у фази.
6. **Ніколи не видаляй** записи з "Журналу виконаних задач" чи "Нових
   знахідок" — цей файл веде історію проєкту, тільки додавай.
7. **Не позначай задачу виконаною**, якщо не протестував зміну хоча б
   локально/на dev-середовищі. Для задач, повʼязаних з деплоєм —
   обов'язково спершу dev, лише потім prod (див. Фазу 1, задача CI-1).

---

## 📊 Поточний стан проєкту (знімок)

Повна методика оцінки, шкала 0-9 і критерії балів — у `SCORECARD.md`.
Тут — стислий знімок для швидкої орієнтації.

| # | Критерій | Бал /9 | Найбільший розрив |
|---|---|:---:|---|
| 1 | Архітектура та структура коду | 7 | `api/` — монолітний файл без роутера |
| 2 | Консистентність і стандарти | 6 | Лінтер не блокує коміт локально (немає pre-commit hook) |
| 3 | Безпека | 7 | Немає формального GDPR-процесу видалення даних |
| 4 | CI/CD і розгортання | 8 | Потрібен test gate (S-6) та prod-середовище з підтвердженням |
| 5 | Тестування | 0 | Тестів немає взагалі |
| 6 | Документація для людей | 5 | Немає інструкцій запуску/env-змінних, README сервісів |
| 7 | Дружність до AI-агентів | 5 | Немає `AGENTS.md`, конвенції не задокументовані |
| 8 | Спостережуваність | 4 | `api/` має логер `apiLog`, але немає зовнішнього моніторингу |
| 9 | Приватність і відповідність | 7 | LICENSE (AGPL v3) + політика приватності + prod/dev розділено |
| 10 | Готовність для стейкхолдерів | 5 | Немає badges, CONTRIBUTING, roadmap |

**Найнижчий бал (Тестування — 0) — це відсутність процесу, а не
складна проблема в коді. Саме тому він йде у Фазі 4 плану нижче.**

---

## ✅ Легенда статусів

| Позначка | Значення |
|---|---|
| ⬜ | Заплановано, ще не почато |
| 🔄 | В роботі зараз |
| ✅ | Виконано |
| ⏸ | На паузі (є причина, описана в примітці) |
| ❌ | Скасовано (є причина, описана в примітці) |

---

## 🗺️ Огляд фаз

| Фаза | Мета | Чому в такому порядку |
|---|---|---|
| **0** | Критичні швидкі фікси | Найвищий ризик / найкраще співвідношення "зусилля-ефект", включно з prod/dev розділенням і базовою юридичною гігієною |
| **1** | Стандарти та процеси | Без лінтера й CI будь-який подальший рефакторинг знову розмиється |
| **2** | Усунення дублювання коду | Швидкі, безпечні правки, що прибирають конкретні розбіжності |
| **3** | Структурний рефакторинг | Більші, ризикованіші зміни — робити вже на стабільній основі (Фази 0-2) |
| **4** | Якість і надійність | Тести, спостережуваність — вимагають стабільної структури з Фази 3 |
| **5** | Документація та відкритість | Найкраще документувати систему, коли вона вже впорядкована |
| **6** | Косметика та довгострокова гігієна | Не блокує нічого, робити у фоновому режимі |

---

## Задачі за фазами

### Фаза 0 — Критичні швидкі фікси

- [x] ✅ **P0-1** — Розділити prod/dev бази даних у `web-admin/wrangler.toml`
  (додати `[env.production]` з `database_id` прод-бази, зараз верхній
  рівень конфігу вказує напряму на dev-базу).
  **DoD:** в конфігу є явні `env.production` і `env.dev`, є скрипти
  `deploy:dev`/`deploy:prod` в `package.json`, перевірено деплоєм на dev.
  **Пріоритет:** Критичний · **Критерій:** #3 Безпека
  *(деталі: `AUDIT.md` → P0-1)*

- [x] ✅ **S-1** — Додати файл `LICENSE` у корінь репозиторію.
  **DoD:** обраний тип ліцензії відповідає намірам власника (проєкт
  закритий комерційно чи відкритий) — узгодити з власником перед
  вибором тексту; файл додано в корінь, README оновлено з коротким
  посиланням на нього замість поточного нечіткого рядка "власність
  BotDev369".
  **Пріоритет:** Критичний · **Критерій:** #9 Приватність/відповідність

- [x] ✅ **S-2** — Додати базову політику приватності / умови використання
  для `web/` (Telegram Mini App), оскільки бот збирає особисті дані
  (дати, ймовірно імена).
  **DoD:** окрема сторінка/файл з описом: які дані збираються, для чого,
  де зберігаються (D1/Telegram), як користувач може попросити видалення.
  Посилання на неї додано в `web/` там, де користувач вводить дані.
  **Пріоритет:** Критичний · **Критерій:** #9 Приватність/відповідність

---

### Фаза 1 — Стандарти та процеси

- [x] ✅ **P0-2** — Додати єдиний ESLint + Prettier конфіг на всі 4 сервіси.
  **DoD:** кореневі `eslint.config.js`/`prettier.config.js`, підключені
  в `bot/`, `api/`, `web/`, `web-admin/`; `npm run lint` і
  `npm run format` працюють у кожному сервісі; один комміт
  "chore: normalize formatting" прогнаний по всьому репо.
  **Пріоритет:** Критичний · **Критерій:** #2 Консистентність
  *(деталі: `AUDIT.md` → P0-2)*

- [x] ✅ **CI-1** — Перехід з нативної Cloudflare Git-інтеграції на
  GitHub Actions з фільтром змінених папок і лінт/білд-гейтом перед
  деплоєм.
  **DoD:** `.github/workflows/deploy.yml` деплоїть лише змінений(і)
  воркер(и), крок `lint`+`build` блокує деплой при помилці; секрети
  (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`) в GitHub Secrets;
  протестовано на dev; нативна Git-інтеграція Cloudflare вимкнена
  для всіх 4 проєктів лише після успішного тесту.
  **Пріоритет:** Високий · **Критерій:** #4 CI/CD
  *(деталі: `AUDIT.md` → CI-1)*
  **Залежить від:** P0-2 (лінт-гейт має що перевіряти)

- [x] ✅ **S-3** — Увімкнути автоматичну перевірку вразливостей залежностей
  (GitHub Dependabot alerts, або `npm audit` як крок у CI з Фази 1).
  **DoD:** Dependabot увімкнений у налаштуваннях репозиторію (Settings →
  Security), або окремий крок `npm audit --audit-level=high` доданий
  у workflow з CI-1.
  **Пріоритет:** Високий · **Критерій:** #3 Безпека

---

### Фаза 2 — Усунення дублювання коду

- [x] ✅ **P1-2** — Прибрати дублікат логіки авто-міграції колонок D1:
  `api/` має власну спрощену версію того, що вже є в
  `bot/src/core/database/auto-migrate.ts`.
  **DoD:** `api/` використовує ту саму (скопійовану або спільну через
  P2-3) функцію `withAutoMigrate`, `ensureMyDatesColumn` видалено.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура
  *(деталі: `AUDIT.md` → P1-2)*

- [x] ✅ **P1-3** — Винести формулу форматування дати
  (`new Date().toISOString().replace(...)`, продубльована в 7 місцях)
  в одну функцію `formatSqliteDatetime()`.
  **DoD:** функція визначена один раз у `bot/src/shared/utils/`,
  усі 5 входжень у `bot/` замінені на виклик; копії функції з тим самим
  ім'ям додані в `api/` і `web-admin/` (або спільний пакет, якщо P2-3
  вже зроблено).
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура
  *(деталі: `AUDIT.md` → P1-3)*

- [x] ✅ **P1-4** — Прибрати потрійне дублювання константи `VALID_TYPES`
  в `api/src/index.ts`.
  **DoD:** одна константа на рівні модуля, `VALID_TYPES_PUT` видалено,
  використовується скрізь той самий масив.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура
  *(деталі: `AUDIT.md` → P1-4)*

- [x] ✅ **P2-4** — Прибрати залишкові `console.log` (7 місць), перевести
  на єдиний логер; додати мінімальне логування в `api/` (там його зараз
  немає взагалі).
  **DoD:** жодного прямого `console.log` у продакшн-коді; `api/` має
  хоча б просту обгортку логера з префіксом `[api]`.
  **Пріоритет:** Середній · **Критерій:** #8 Спостережуваність
  *(деталі: `AUDIT.md` → P2-4)*

---

### Фаза 3 — Структурний рефакторинг

- [ ] ⬜ **P1-1** — Розбити `api/src/index.ts` (612 рядків) на роутер +
  контролери за прикладом `bot/src/api/`.
  **DoD:** структура `api/src/router.ts` + `api/src/controllers/*`,
  `api/package.json` і `api/tsconfig.json` додані; `index.ts` містить
  лише `export default { fetch }`.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура
  *(деталі: `AUDIT.md` → P1-1)*
  **Залежить від:** P1-2, P1-3, P1-4 (легше рефакторити файл без
  дублікатів усередині)

- [ ] ⬜ **P2-2** — Розбити `web/src/pages/MyDatesPage.tsx` (958 рядків)
  на `features/my-dates/` (types, constants, utils, хук, під-компоненти).
  **DoD:** сторінка `MyDatesPage.tsx` — тонка обгортка, логіка й JSX
  розкидані по `features/my-dates/` за прикладом структури `web-admin/`.
  **Пріоритет:** Середній · **Критерій:** #1 Архітектура
  *(деталі: `AUDIT.md` → P2-2)*

- [ ] ⬜ **P2-3** — Ввести npm workspaces + `packages/shared/` для коду,
  спільного між сервісами (дата-форматування, авто-міграція, спільні типи).
  **DoD:** кореневий `package.json` з `workspaces`, `packages/shared/`
  містить перенесені спільні функції, всі 4 сервіси імпортують звідти
  замість локальних копій.
  **Пріоритет:** Середній · **Критерій:** #1 Архітектура
  *(деталі: `AUDIT.md` → P2-3)*
  **Залежить від:** P0-2 (щоб новий пакет одразу дотримувався стандартів)

---

### Фаза 4 — Якість і надійність

- [ ] ⬜ **S-4** — Підключити тестовий фреймворк (рекомендація: Vitest —
  добре працює з Vite/Workers) хоча б у `bot/` і `api/`.
  **DoD:** `vitest` доданий у `devDependencies`, є `npm run test` у
  відповідних `package.json`, налаштований мінімальний конфіг.
  **Пріоритет:** Високий · **Критерій:** #5 Тестування

- [ ] ⬜ **S-5** — Написати перші тести для найкритичнішої бізнес-логіки:
  розрахунок гороскопів (`calculateWesternAstrology` в `api/`),
  валідація дат/типів (`VALID_TYPES` з P1-4), авто-міграція БД.
  **DoD:** мінімум 5-10 тестів, що покривають критичні кейси й хоча б
  одну відому граничну умову (напр. 29 лютого, межі знаків зодіаку).
  **Пріоритет:** Високий · **Критерій:** #5 Тестування
  **Залежить від:** S-4

- [ ] ⬜ **S-6** — Додати крок `npm run test` у workflow з CI-1, щоб
  тести блокували деплой так само, як лінт.
  **DoD:** workflow не деплоїть, якщо тести не проходять.
  **Пріоритет:** Середній · **Критерій:** #4 CI/CD, #5 Тестування
  **Залежить від:** CI-1, S-4

- [ ] ⬜ **P2-1** — Поступово зменшувати використання `any` (101 випадок),
  почати з `api/` під час його розбиття (P1-1).
  **DoD:** ESLint-правило `@typescript-eslint/no-explicit-any` у режимі
  `warn` додано в конфіг з P0-2; кількість `any` в `api/` зменшена
  щонайменше вдвічі після P1-1.
  **Пріоритет:** Середній · **Критерій:** #2 Консистентність
  *(деталі: `AUDIT.md` → P2-1)*

- [ ] ⬜ **S-7** — Додати зовнішнє відстеження помилок (напр. Sentry —
  має офіційну підтримку Cloudflare Workers) хоча б для `bot/` і `api/`.
  **DoD:** необроблені помилки в проді автоматично потрапляють у
  зовнішній сервіс з алертом, а не губляться в логах Cloudflare.
  **Пріоритет:** Середній · **Критерій:** #8 Спостережуваність

---

### Фаза 5 — Документація та відкритість

- [ ] ⬜ **S-8** — Додати `AGENTS.md` у корінь репозиторію з описом
  конвенцій проєкту (де що лежить, чому `bot/` організований саме так,
  доменні терміни: Scenario, Family Box, Rich Message, Forum Topics).
  **DoD:** файл існує, описує архітектурні рішення й "не роби так"
  (напр. "не пиши власну авто-міграцію, використовуй
  `withAutoMigrate` з `packages/shared`").
  **Пріоритет:** Середній · **Критерій:** #7 Дружність до AI-агентів

- [ ] ⬜ **S-9** — Додати інструкції "Getting Started" в корінний README:
  як запустити кожен сервіс локально, які env-змінні потрібні (без
  значень — лише назви й призначення), як задеплоїти на dev.
  **DoD:** новий розробник або AI-агент може підняти проєкт локально,
  користуючись лише README.
  **Пріоритет:** Середній · **Критерій:** #6 Документація

- [ ] ⬜ **S-10** — Додати короткий `README.md` всередину кожного з 4
  сервісів (`bot/`, `api/`, `web/`, `web-admin/`) з описом саме цієї
  частини.
  **DoD:** 4 файли README, кожен описує призначення сервісу, основні
  ендпоінти/команди, залежності від інших сервісів.
  **Пріоритет:** Низький · **Критерій:** #6 Документація

- [ ] ⬜ **S-11** — Додати `CONTRIBUTING.md`, badges у README (build
  status, license), почати вести `CHANGELOG.md`.
  **DoD:** видимі badges у README, є базовий процес для контриб'юторів,
  CHANGELOG починається з поточної версії.
  **Пріоритет:** Низький · **Критерій:** #10 Готовність для стейкхолдерів

---

### Фаза 6 — Косметика та довгострокова гігієна

- [ ] ⬜ **P3-1** — Уніфікувати підхід до стилів у `web-admin`
  (1830 рядків ручного CSS поряд з Tailwind 4).
  **Пріоритет:** Низький · **Критерій:** #10 Готовність для стейкхолдерів
  *(деталі: `AUDIT.md` → P3-1)*

- [ ] ⬜ **P3-2** — Вирівняти `compatibility_date` в усіх 4 `wrangler.toml`.
  **Пріоритет:** Низький · **Критерій:** #4 CI/CD
  *(деталі: `AUDIT.md` → P3-2)*

- [ ] ⬜ **P3-3** — Зафіксувати політику вирівнювання версій
  React/Vite/TypeScript між `web` і `web-admin` при наступному
  плановому оновленні.
  **Пріоритет:** Низький · **Критерій:** #10 Готовність для стейкхолдерів
  *(деталі: `AUDIT.md` → P3-3)*

---

### Позапланові задачі — web-admin: Сторінка «Користувачі»

> Задачі додано 27.08.2026 на прохання власника. Не входять у фази 0-6,
> але пов'язані з розвитком web-admin як адмін-інструменту.

- [ ] ⬜ **WA-U1** — Додати API-ендпоінти в `web-admin/src/worker.ts`
  для роботи з таблицею `users`.
  **Ендпоінти:**
  - `GET /api/users/list` — список усіх користувачів (без heavy полів
    на кшталт `my_dates` за замовчуванням)
  - `POST /api/users/read` — читання одного користувача за `user_id`
  - `POST /api/users/update` — оновлення довільних полів користувача
  - `POST /api/users/delete` — видалення користувача
  - `POST /api/users/message` — відправка повідомлення користувачеві
    через Telegram Bot API (потрібен `BOT_TOKEN` в Env)
  **DoD:** ендпоінти працюють, захищені admin-аутентифікацією,
  `GET /api/users/list` повертає всі колонки крім `my_dates`.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U2** — Додати API-ендпоінти для блокування/розблокування
  та масових дій.
  **Ендпоінти:**
  - `POST /api/users/block` — заблокувати користувача (додати
    поле `is_blocked` або використати KV)
  - `POST /api/users/unblock` — розблокувати
  - `POST /api/users/bulk` — масові дії (видалити, заблокувати,
    розблокувати список `user_id`)
  **DoD:** bulk-ендпоінт приймає `{ action: "delete"|"block"|"unblock",
  ids: number[] }`, повертає кількість оброблених.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U3** — Додати Zustand-store для користувачів
  (`web-admin/src/features/users/store.ts`).
  **DoD:** store з `items`, `selectedIds`, `load()`, `sort`, `filter`,
  `toggleSelect`, `selectAll`, `bulkAction`.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U4** — API-клієнт для користувачів
  (`web-admin/src/shared/api/users.api.ts`).
  **DoD:** функції `listUsers`, `readUser`, `updateUser`, `deleteUser`,
  `sendMessage`, `blockUser`, `unblockUser`, `bulkAction` —
  всі через `apiFetch`.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U5** — Сторінка «Користувачі» — таблиця з даними.
  **Компоненти:**
  - `UsersPage.tsx` — основна сторінка
  - `UsersTable.tsx` — таблиця (sticky header, sticky колонки 1-3)
  - `UserRowMenu.tsx` — контекстне меню (modal): переглянути,
    редагувати, видалити, заблокувати
  - `UserBulkBar.tsx` — панель масових дій (з'являється при виборі)
  **Стовпці (порядок):** меню (☰), чекбокс, повне ім'я, username,
  user_id, дата приєднання, дата останньої активності, інші поля.
  **DoD:** таблиця рендерить дані з API, працює на мобайлі.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U6** — Сортування та фільтрація в таблиці.
  **DoD:** клік по заголовку стовпця — сортування ASC/DESC;
  пошуковий рядок для фільтрації по username/імені.
  **Пріоритет:** Середній · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U7** — Модалка «Повідомлення користувачеві».
  **DoD:** при натисканні «Написати» в меню користувача —
  відкривається модалка з полем тексту та кнопкою «Надіслати»;
  викликає `POST /api/users/message`.
  **Пріоритет:** Середній · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U8** — Мобільна адаптація таблиці користувачів.
  **DoD:** на екранах < 768px — горизонтальний скрол,
  sticky перша колонка (меню+чекбокс), стислий формат.
  **Пріоритет:** Середній · **Критерій:** #1 Архітектура

- [ ] ⬜ **WA-U9** — Додати «Користувачі» в навігацію sidebar.
  **DoD:** новий пункт `[{ to: "/users", label: "Користувачі",
  icon: "👥" }]` в `navItems.ts`, маршрут в `router.tsx`.
  **Пріоритет:** Високий · **Критерій:** #1 Архітектура

**Залежності між задачами:**
WA-U1 → WA-U2 → WA-U4 → WA-U3 → WA-U5 → WA-U6
\_ → WA-U7
\_ → WA-U8
WA-U9 (незалежна, можна зробити будь-коли)

---

## 🧾 Журнал виконаних задач

> Додавати новий рядок унизу таблиці після кожної завершеної задачі.
> Не редагувати й не видаляти попередні рядки.

| Дата | ID | Що зроблено (коротко) | Виконавець (модель/людина) | Коміт/PR |
|---|---|---|---|---|
| 26.08.2026 | — | Створено `AUDIT.md`, `SCORECARD.md`, `PROJECT_PLAN.md` | Claude (в чаті) | — |
| 26.08.2026 | P0-1 | Розділено prod/dev бази у `web-admin/wrangler.toml`, додано `deploy:dev`/`deploy:prod` скрипти | Buffy (AI-агент) | — |
| 26.08.2026 | S-1 | Додано LICENSE (AGPL v3), оновлено README з посиланням на ліцензію | Buffy (AI-агент) | — |
| 26.08.2026 | S-2 | Додано сторінку політики приватності `web/public/privacy.html`, посилання в AppFooter | Buffy (AI-агент) | — |
| 26.08.2026 | P0-2 | Додано єдиний ESLint+Prettier конфіг на всі 4 сервіси, .editorconfig, форматування 149 файлів | Buffy (AI-агент) | — |
| 26.08.2026 | CI-1 | Створено `.github/workflows/deploy.yml` з path filtering, lint gate, npm audit. Додано Dependabot | Buffy (AI-агент) | — |
| 26.08.2026 | S-3 | Увімкнено Dependabot для всіх 4 сервісів + GitHub Actions | Buffy (AI-агент) | — |
| 27.08.2026 | CI-1 | Виправлено workflow: `--env dev` замість `--env production`, прибрано eslint з deploy jobs (помилка npx), оновлено Account ID в GitHub Secrets | Buffy (AI-агент) + BotDev369 | `d960fc6` |
| 27.08.2026 | — | Деплой bot-воркера на Cloudflare успішно працює через GitHub Actions | Buffy (AI-агент) + BotDev369 | — |
| 27.08.2026 | P1-4 | Об'єднано 3 копії `VALID_TYPES` в `api/src/index.ts` в одну модульну константу, видалено `VALID_TYPES_PUT` | Buffy (AI-агент) | — |
| 27.08.2026 | P1-3 | Створено `formatSqliteDatetime()`, замінено 8 входжень у `bot/`, `api/`, `web-admin/` | Buffy (AI-агент) | — |
| 27.08.2026 | P1-2 | Скопійовано `withAutoMigrate` з `bot/` в `api/src/shared/`, видалено `ensureMyDatesColumn` з `api/src/index.ts` | Buffy (AI-агент) | — |
| 27.08.2026 | P2-4 | Додано логер `api/src/shared/logger.ts`, замінено 8 `console.log`/`console.error` в `api/src/index.ts` на `apiLog` | Buffy (AI-агент) | — |

---

## 🔎 Нові знахідки

> Сюди AI-агенти додають проблеми, помічені під час роботи над задачею,
> але не описані в плані. Власник проєкту вирішує, чи й куди їх додати
> у фази вище. Формат: дата, короткий опис, пропонований пріоритет.

*(поки порожньо)*

---

## 📌 Наступний перегляд оцінки

Рекомендація з `SCORECARD.md`: переоцінювати всі 10 критеріїв раз на
4-6 тижнів активної роботи над планом, щоб бачити реальний прогрес, а не
відчуття "ми ніби щось робимо". Наступний запланований перегляд:
**орієнтовно жовтень 2026** (після завершення Фаз 0-2).

---

## Пов'язані документи

- `public docs/AUDIT.md` — детальний код-аудит з поясненнями "чому" і
  прикладами коду для кожної задачі з префіксами `P0-P3` та `CI-1`.
- `public docs/SCORECARD.md` — повна методика оцінки (що означає кожен
  бал 0-9 для кожного з 10 критеріїв) і чесна оцінка станом на 26.08.2026.
