# web-platform-dev

Cloudflare Worker: Telegram Mini App для wwwuabot (React 19 + Vite 8 + Tailwind 4).

## Налаштування

- **Service binding:** `API` → `api-dev` (увесь трафік `/api/*` іде туди)
- **D1:** немає — прямувати до бази цей воркер не має права
- **Секрети:** не потрібні (авторизація — підписаний Telegram `initData`)
- **Середовище:** `ENVIRONMENT = "dev"` — задеплоєний лише дев-воркер, прода немає

## Мобільний — пріоритет №1

Це єдина точка взаємодії користувачів, і майже завжди — телефон. Тому в цьому
застосунку перевіряють **спочатку 360px**, а десктоп — потім. Обовʼязково:

- резинова верстка (`flex`/`grid`, `clamp()`, без фіксованих ширин розкладки);
- `100dvh` замість `100vh` (адресний рядок ховається й висота «стрибає»);
- `var(--safe-top)` / `var(--safe-bottom)` на краях екрана — працюють лише з
  `viewport-fit=cover` у `index.html`;
- тап-таргети ≥ 44px і афорданси без `:hover` (`@media (hover: none)`);
- **ніяких** `alert` / `confirm` / `prompt` — на iOS вони не показуються взагалі,
  тільки `useDialog()` з `@wwwuabot/ui/dialog`.

## Роути

| Шлях | Що це |
|---|---|
| `/` | головна — сценарій `__base__` |
| `/sites`, `/sites/new`, `/sites/:slug` | мої сайти, створення, редактор |
| `/catalog`, `/view/:slug` | публічний каталог і перегляд сайту |
| `*` | catch-all: сценарій за `web_slug` / codeword |

---

**Останнє оновлення:** 2026-09-12
