/**
 * Формат часу нотатки — обгортка над спільним правилом.
 *
 * Сам розбір жив тут до 17.09.2026 — і це була **та сама** копія, що потрібна
 * будь-якому списку з датою (контакти, заявки, історія). Дві копії такого
 * розбору розійшлися б тихо: один список показував би час зі зсувом зони, а
 * інший — ні. Тепер правило одне (`@wwwuabot/shared/utils/datetime`), а імена
 * нотаток лишились: список нотаток читається як «штамп нотатки», і міняти
 * назви в розмітці заради переїзду коду не варто.
 *
 * @module @wwwuabot/ui/notes
 */

export {
  formatStamp as formatNoteStamp,
  sqliteTimestamp as noteTimestamp,
} from "@wwwuabot/shared/utils/datetime";
