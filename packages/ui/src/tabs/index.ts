/**
 * @wwwuabot/ui/tabs — вкладки «два погляди на те саме».
 *
 * Одна розмітка на розділи акаунта й на розділи Простору: смуга на всю ширину,
 * рівні половини, вибрана залита акцентом (кирпичик кнопки, а не власний
 * вигляд).
 *
 * @module @wwwuabot/ui/tabs
 */

export { Tabs, tabId, tabPanelId } from "./Tabs";
export type { TabOption } from "./Tabs";
