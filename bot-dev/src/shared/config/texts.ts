/**
 * Тексти повідомлень бота.
 *
 * `ENVIRONMENT` береться з `wrangler.toml` і має рівно одне прод-значення —
 * `production` (так само, як в `api-dev`). Раніше тут порівнювалось `"prod"`,
 * а у `wrangler.toml` стояло `"dev"`, тож **у проді користувачі бачили
 * «Сталася помилка (Dev mode)»**. Два різні слова для одного стану — саме та
 * дрібниця, яку не ловить ні typecheck, ні око.
 *
 * Це ж значення їде в Sentry як `environment`, тому розбіжність плутала б ще
 * й фільтри помилок у панелі.
 */

/** Єдине значення `ENVIRONMENT`, яке означає прод. */
const PRODUCTION = "production";

export const TEXTS = {
  error: (env: string) =>
    env === PRODUCTION
      ? "Вибачте, сталася технічна помилка. Ми вже працюємо над її вирішенням. 🛠"
      : "⚠️ Сталася помилка (Dev mode). Деталі в логах Cloudflare.",
};
