/**
 * Відомий борг, який перевірка `scripts/check-css-classes.mjs` поки що терпить.
 *
 * Правило просте: сюди потрапляє тільки те, що вже описано в документації як
 * відкрита робота. Усе інше — нова помилка, і гейт її зупинить.
 *
 * Зменшувати цей файл — і є планом: кожен прибраний рядок = закритий пункт.
 */
export const BASELINE = {
  /**
   * `wb-block-*` — блоки Page Builder. 44+ класів рендерить спільний
   * `packages/ui/src/blocks/*`, а правил для них немає: блоки стилізовані
   * інлайном (`style={{…}}`), тому `data-brand` і `data-theme` до них не
   * доходять, а `apple.css`/`android.css` на блоки не впливають узагалі.
   * Відкрита робота: `docs/CONSOLIDATION_PLAN.md` §3 пункт 3 — переносити
   * порціями по 5–8 блоків.
   */
  sharedUnstyledPatterns: [/^wb-block-/],
};
