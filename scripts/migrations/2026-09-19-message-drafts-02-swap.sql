-- ═════════════════════════════════════════════════════════════════════════════
-- Міграція `message_drafts` — крок 2 з 2: заміна таблиці.
--
-- Запускати **тільки після** `2026-09-19-message-drafts-01-copy.sql` і перевірки,
-- що звіт кроку 1 показав однакові числа:
--
--   SELECT COUNT(*) FROM message_drafts;       -- стара
--   SELECT COUNT(*) FROM message_drafts_new;   -- нова
--
-- Стара таблиця **не видаляється** — перейменовується в
-- `message_drafts_legacy_20260919`. Це бекофісний хід, а не нігілізм: дані
-- лишаються на місці, і крок оборотний одним `ALTER TABLE … RENAME TO`. Коли
-- власник переконається, що все на місці, легасі-таблиця прибирається однією
-- командою з Dashboard (D1 тримає Time Travel на 30 днів).
--
-- Другою дією **переставляється `AUTOINCREMENT`**: послідовність нової таблиці
-- уже знає свій максимум (рядки перенесені з номерами), тож наступна чернетка
-- отримає номер після перенесених, а не поверх них.
--
-- Індекс створюється тут, а не `ensureTables`: ім'я `idx_drafts_owner`
-- звільнилось у кроці 1, і без цього рядка нова таблиця лишилась би без
-- індексу — а `CREATE INDEX IF NOT EXISTS` на легасі-таблиці виглядав би як
-- успіх, не роблячи нічого.
-- ═════════════════════════════════════════════════════════════════════════════

ALTER TABLE "message_drafts" RENAME TO "message_drafts_legacy_20260919";
ALTER TABLE "message_drafts_new" RENAME TO "message_drafts";

CREATE INDEX IF NOT EXISTS "idx_drafts_owner" ON "message_drafts"(owner_id);

-- Звіт: нова таблиця на місці, номери й адресати збережені.
SELECT COUNT(*) AS чернеток, MAX(id) AS найбільший_номер FROM "message_drafts";
